# PROMPT: Phase 3 (Execute)

## Status

- **Phase:** 3 of 4. (Updated 2026-09-21.)
- **Milestone:** M14 next (`docs/plan/14-wire-framing.md`; After 12b, ready), then M15 (After 13+14). M10 is done, so **CI now runs on every push to `main`** and is the only thing proving three-browser determinism and the production pages' `burst` controls (ADR 0026). **Push every `done` commit** — Tyler authorised pushing without asking (M11 session: "Just push. No approval necessary") — and read the run: a red CI after a green local gate is a real finding, as M10 proved four times over.
- **State:** M01–M13 are done and on `main` (tag `harness-complete` at M04's `done` commit); `pnpm test && pnpm lint` is green (four suites: `rust` 191, `unit` 154, `wasm` 40, `browser` 95; **`browser` is at 19 s of 25 s quiet and touched 25 s under `--load 10`** — the wire is armed at 20 s (`deferred-ledger.md`) and M14 is likely to cross it, after M09's gate moved the WebKit/Firefox repeats to the slow-tier `engines` leg and generated `burst` negatives to `@slow`, ADR 0026: **three-browser determinism is now proven by `pnpm test:slow` and M10's CI, not by `pnpm test`**). Phase 3 runs as one orchestrating session with Sonnet implementers on `main` (ADR 0025). **M04–M08b are settled; each brief's Deviations hold their exact seam shapes, so only the names are here.** M04: the zero-allocation instrument (`tests/browser/gc/`, `zeroGcSuite`, page `gc-loop` with five permanent negative controls, tunnel and flat CDP transports), `budgets.json` + `tests/support/budgets.ts`, `installGcPage`, `pnpm gc <software|flat|reliability>`, the `gc-test` skill. M05: `engine::codec` (`Codec`, canonical NaN, `decode_canonical`), `engine::bytes` (`ByteSink`, `ByteReader`, the one varint), `StateHash` / `hash_value` / `mix64`, byte goldens (`assert_golden_bytes!`, `pnpm golden:bytes`). M06: `src/sab/` (SPSC ring, seqlock, triple buffer, control block, `SabSet` / `createSabSet`, `RING_DEFAULTS`) and `src/camera/{state,block}.ts`, after two fix rounds for a livelock. M06b: `createClient` / `checkSupport` / `EngineStartError`, `engine/worker` `run()` with the four kinds, the `yield` protocol (`runBlockingLoop(shell, body, timeoutMs, lastSeen?)`, `Shell.observeWake()`), the `frame` export, the production-topology helpers in `engine/test`, zero-GC pages `topology` and `echo`, after three fix rounds (per-pass HeapNumber boxing, a lost wake on resume, lazy-feedback allocation in warm-up); its Deviations end with "Notes for later briefs". M07: `engine::world` (`Tile`, coords, `ChunkDims`, `Registry`, `PristineSource`, `TerrainStore` with canonical overlays over an invisible LRU dense cache), `engine::testing::assert_cache_invisible`. M08: `engine::worldgen` (`Worldgen`, `hash2`, `Pristine<W>`, `worldgen_fingerprint`, `WorldgenStamp`, `GenCore`), `engine::noise`, `gen_chunk` (`ABI_VERSION` 3), `fx-worldgen` with a six-runtime golden, slow-tier `worldgen-bench`. M08b: `engine::gen_queue` / `engine::view` / `client::TerrainFeed`, client exports `gen_take` / `gen_deliver` / `client_gen_stats` / `client_chunk_hash` (`ABI_VERSION` 4), the gen worker body and client pump over `genRequest` / `genResult`, `runBlockingLoop` draining on entry, the test-gated parked `test-call` channel (`callParked`, `gen.stats` / `gen.idle` / `gen.chunkHash`), pages `gen` and `gc-gen`; `buildGame` writes its outputs atomically. M09 landed the terrain data path (`client::{texel,upload}` with `TileTexel` / `ClientSide` / `Uploader`, client export `upload_stage`, `uploadRing.slotBytes` 4120; `src/render/{device,art,terrain,upload}.ts`, `terrain.wgsl` embedded by `scripts/embed-wgsl.mjs`, `frame-loop.ts`; `fixtures/terrain`; `renderTo` / `readPixels` / counters in `engine/test`; pages `terrain`, `terrain-client`, `gc-terrain`), built by three implementers plus a Sonnet review of its 5,200-line diff and three fix rounds; **its standing reservation about making recompilation synchronous is superseded by 0028 and must not be acted on** — M11 established that the lump was the one-off JIT code-installation burst, that `terrain`'s `extraSettleFrames: 500` fixed it by luck and is now inert, and that every warm-up knob merely relocates the phase; exact seam shapes are in each brief's Deviations; M09b landed the shader's final form (PCG3D hash → variant/flip/rotate/jitter, Bayer edge dithering, fat-pixel magnification, trilinear mips via `render/mips.ts`), `render/viewport.ts` (`ResizeObserver` + render scale + backgrounding, `createViewportController`), the first real wiring of `createFrameLoop` to a canvas (`createRealFrameLoop`; `FrameLoop.pause()`/`resume()`), `ClientOptions.render`, `engine/test`'s `setViewport`/`setVisibility`, and `device.html` + HUD, built by three implementers plus two fix rounds. **Carry forward:** magnified sampling must anchor on `floor(texel + 0.5)`, not `floor(texel)` — it was inverted from M09 until M09b's review found it, invisible because every probe sat exactly at a texel centre where both formulas agree; `terrain: seam matches reference` now probes a fractional offset and guards it, and **M17b reuses this sampling path for sprites** (its brief carries the warning). M11 landed the engine-owned camera (`camera/{transform,camera}.ts`), the fixed-slot listeners (`input/{pointers,keys,wheel}.ts`: **listeners record, rAF integrates**), semantic events and `client.input`, the `inputRing` → `on_input` → Rust `InputQueue` path (**`ABI_VERSION` 6**), `moveTo`/constraints/`localStorage` persistence, the zero-GC page `input`, and `device.html`'s gestures and `?probe=memory`; built by three implementers plus **four fix rounds**, the last two on an Opus implementer, whose reading of the raw `profile.samples` found **a one-off V8 JIT code-installation burst billed to whichever JS frame happens to be executing** (seven different frames were observed, which is why no name-based exclusion could work). **ADR 0028** (superseding 0027, amending 0016 §3 step 7) makes assertion B take the lower of two consecutive 600-frame windows and re-derived every page's `main` budget downward; **the strict worker figure stayed 8 throughout — what is counted changed, never what is allowed.** **M10 landed CI** (`.github/workflows/ci.yml`: one `ubuntu-latest` job, push-to-`main` + PR, `cancel-in-progress`, actions pinned by SHA; `pnpm lint` + both tiers under `CI=true ENGINE_GPU=swiftshader GC_MODE=software`; `--timings-json` on the runner, timings recorded never gating via `--budget-scale 1000`, artefact + job summary). **Spike B answered positively**: `channel: 'chromium'` (new headless) gives a **null adapter** on the runner, `channel: 'chromium-headless-shell'` gives `{vendor: google, architecture: swiftshader, isFallbackAdapter: true}` — fallback rung 1, rungs 2-4 never needed — and **every M09 readback scene, probe and `device.html` check passed with no golden mismatch at all**, so no per-adapter-class golden exists. x86-64 determinism (0002) and recursive `fs.watch` on Linux are **closed** (three-browser determinism green on the runner: WebKit + Firefox match the Apple-silicon goldens). Its real work was **ADR 0029**: turning on `GC_MODE=software` ran 0016 caveat b's attribution mechanism for the first time ever and it did not work — control hooks fired as *siblings* of their attribution roots, so every worker control and `main`'s control on all five pages attributed 0 against raw counters that saw the allocation. Hooks now **nest inside the root** (never widen a root to reach a control), and because V8 inlined `body()` away on `topology`'s `client` 5/5 (0.0267 B/frame attributed vs 16 real), **software mode attributes only `main`; every other isolate uses raw bytes in both modes**. `software` blocks, `frames: 600`, `main`-only: `topology` 20, `echo` 8, `gen` 20, `terrain` 89, `input` 169. **The nested control is now a live inlining detector: a control that stops tripping is a signal about the instrument, never something to fix by widening a root or relaxing a budget.** Also: `@webkit-gpu` runs everywhere but Linux (WebKitGTK ships no WebGPU — asserted against the platform, never discovered from a missing `navigator.gpu`, which would make the test unable to fail); the `gc` project's per-test timeout is 90 s under `ENGINE_GPU=swiftshader` only (measured: 30,655 ms timed out, 29,893 ms passing, CI/local ratio 3x-19x by contention); a temp fixture crate was building with the runner's default Rust 1.98.1 instead of the 1.93.0 pin and now copies `rust-toolchain.toml` in. Runner drift baseline for future comparison: fast `browser` 103 s/25 s, slow `browser` 131 s, `wasm` slow 36 s/7 s, full two-tier cold job ~5.5 min, and `ubuntu-latest`'s CPU varies run to run (Xeon 8370C vs EPYC 7763), so that alone is not a regression signal. **Open on the ledger, not fixed:** one `wasm` runner exit-1 with a passing report (run 35619437805), never reproduced; and whether `main` needs attribution at all under 600 frames (M36b) — dropping it would delete the inlining exposure rather than police it. **M12 landed the data side of the game API** in one implementer, no fix rounds: the **full** `Game` trait of 0003 with every associated item declared up front — shells with no-op defaults for `TickCx` / `FrameCx` / `FrameView` / `DrawList` / `PresenceTable` / `OldStore` / `SaveIncompatible` (associated type defaults are unstable, so declaring the whole trait once is cheaper than breaking every fixture later) — plus `Presence`, `Unknown`, `PlayerEvent`, `Tick`/`Ticks`/`TickRate`, `PlayerId`/`EntityId` (0022), `Delta<G>` with the engine-only `Roster` variant (0024 §8), `Store<G>` over M07's `TerrainStore` with `apply` as its only mutator, canonical `encode`/`decode` + `StateHash`, `SimRng` (PCG32-XSH-RR, golden sequence pinned), and fixture `fx-puts` types. `ts-rs` is now a real dependency of `engine` / `fx-terrain` / `fx-puts` (pre-approved by 0017 §7: nothing of it is reachable from an export, LTO removes it, the size test watches it). **Seam change later briefs should know:** M09's `ClientSide<G = ()>` lost its `= ()` default and gained `G: Game` + `Default`, as M12's brief planned — but `Default` could not be satisfied by `FixtureTerrain` (built from `Instance::init`'s role args), so `fixtures/terrain` and `client/upload.rs`'s test fixture now pass a small local `Vis`/`NoGame` shim as `Uploader`'s type arguments. No `Provides` name was renamed and no later brief writes `Uploader<…>` explicitly, so nothing downstream needed editing; the rejected alternative (split `tile_visual` into its own trait) is in M12's Deviations if a cleaner design is ever wanted. `Store` deliberately holds no `Tick`/`SimRng` (those are Authority's, M12b); canonical order is player table → id counters → `Global` → terrain → entities, `Global`'s placement being the implementer's choice since 0005 does not name it; `PlayerId(pub u32)` follows `EntityId`'s shape by analogy (0022 §1 is literally about `EntityId` only) — both recorded as readings, not as decisions. **M12b landed the execution half** in one implementer, no fix rounds: `WorldRead`/`WorldWrite` (object-safe, upcasting test), `Authority<G>` (a write = `Store::apply` + one scoped `(Scopes, Delta)` on a reused `ChangeLog`; tile → `Chunk`, entity → `Chunk(chunk_of(G::anchor(e)))`, player → `Player(who)`, global/roster → `Global`), `View`, minimal `TickCx` (a struct, index-based so rules can write while iterating), `TickRate::{millis, secs, dt}`, `Record`/`Outcome`/`EngineReject`, `Sim<G>` running records in 0004 order, `fx-puts` handlers, and native-blessed goldens `puts_idle_100` = `195e71ef0defbf7a` / `puts_script_a` = `7bdddfc9c749b1fb` (**M13 makes the first `.wasm`-authoritative, M16 the second — do not re-bless them by hand**). New build step **`doctests`** in `scripts/suites.mjs` (`cargo test --doc -p engine`) because nextest runs no doc tests: without it `TickRate::hz`'s two `compile_fail` blocks would never execute — verified by the orchestrator directly, 2 compile-fail + 1 passing doctest really run. **Three seam notes:** `Delta` gained a 7th variant `Ack` (bumps `last_seq` through `Store::apply`, since `apply` is the only mutator; bypasses the `ChangeLog`, never rebroadcast, so it never reaches M14's wire); M12's `TickRate::hz()` getter is now **`hz_value()`** because 0006's `hz` is the const constructor (no later brief called the getter); and **`WorldRead::entity_at` is `Ok(None)` until M21**, so `fx-puts`'s `Bump`/`Remove` reject `NotFound` **by design** — a later implementer must not read that as a bug. `Sim::genesis` carries a local `where G::Global: Default` rather than widening `Game`. **M13 landed the sim host and tick loop**, cut 1–3 / 4–6 up front (cross-thread): `host::Host<G>`, `GameInstance<G>` dispatching on `Role`, `host::warm` (nearest-first, hash-invisible), **`ABI_VERSION` 6 → 8** (`sim_genesis`, `sim_seal_frame`, `sim_warm_one`, `tick_hz`), `SimHost` in `src/server.ts` (no `node:`/DOM imports) with `start`/`stop`/`pause`/`resume`/`stepTick`/`hash`/`counters`/`logSink`, `AtomicsTimer` over `runBlockingLoop`, the `sim` worker kind with a new `CB_SIM_STEP_REQ` word, `engine/test`'s `stepTick`/`worldHash`/`simCounters`, and the `gc-sim` page. **`puts_idle_100` is now `.wasm`-authoritative** in `fixtures/puts/golden/golden.json` and the native Rust test reads the *same* file, so native-vs-wasm equality is now checked continuously rather than once at bless time; the value never moved (`195e71ef0defbf7a`). `puts_script_a` stays native until M16. **Orchestrator decision:** the implementer found `SimHost` pacing at a hardcoded 20 Hz with nothing reading `Game::TICK_RATE`, so a game declaring another rate would silently mispace; I had it add the `tick_hz` export in-milestone (cheap while the ABI bump was already open) with a test that a fake 40 Hz instance really paces at 25 ms. Two real bugs were found in **already-accepted** code: `wrapEngineInstance.simSealFrame` allocated a fresh object per tick (16.8 → 0.81 B/frame on `sim`), and driving real ticks from `asHarness.stepTick()` broke three existing pages that use `fixtures/hash` (no `sim_genesis`) — reverted in favour of a separate `stepSimTickSync`. **Counters are not all live:** `ticksRun` is; `ticksDropped`/`tickOverruns` only in unit tests; `chunksWarmed` is unreachable until M15's `set_view`; **`genOnMiss` is permanently 0** (no export) — do not read any of the last three as coverage. `cacheChunks` is parsed but inert (left so deliberately: M07's cache-invisibility invariant makes a default capacity safe). **A `gc-sim` software budget of `main: 0` is real, not vacuous** — the implementer first explained it as `drive` being inlined away, which is ADR 0029's exact failure mode and would have made it vacuous; the orchestrator measured `sim neg object main` in software mode, it **trips**, and an `object` control causes no GC events by design, so it can only have tripped on assertion B. `main` attributes ~0 because on that page the tick work lives in the **sim worker isolate** and `drive()` only pokes `CB_SIM_STEP_REQ`. Deviations carries the corrected reasoning. Orchestrator repeats: `browser` **30/30 quiet and 30/30 under `--load 10`**, 0 failures, 0 hangs. Known: commits `5cbf35e` (`M03 done`) and `e6767d6` are red on one `unit` test (a `CLAUDE.md` line cap, the orchestrator's slip; fixed in M04), so do not push those two as heads. Device checks ready for Tyler: M03 determinism page, M08 worldgen ms per chunk, **M09b terrain fill rate**, **M11 boot / gestures / memory** and **M11 desktop-Safari trackpad pinch** (`docs/plan/device-checks.md`; M09b's and M11's are run in the same sitting, and M11's `&touch=1` has a known gap recorded in its own item; M09b's is the first that needs a phone and a tunnel: `pnpm device:serve --tunnel`, then `device.html?autopan=1&tiles=256&scale=2`). Open questions for Tyler (none blocking; defaults stand): `docs/plan/questions-for-tyler.md`.
- **Exact next step:** On a new machine run `pnpm install && pnpm setup:tools` first. Start the session on `main` with edits auto-accepted, confirm `milestone-implementer` is among the agent types, then follow the loop from step 1. **Pushing no longer prompts Tyler** — he authorised it in the M11 session; push every `done`-green head, and verify green *before* pushing (the M11 session pushed verified-green range tips mid-milestone too, which is fine, but `5cbf35e`/`e6767d6` are red precisely because an orchestrator committed a record edit without re-running). **CI now runs on every push, so push each `done` commit and then read the run** — since ADR 0026 it is the only proof of three-browser determinism and of the production pages' `burst` controls, and M10 saw it catch four things a green local gate did not: a fixture crate compiled by the runner's default Rust instead of the pin, WebKitGTK shipping no WebGPU, burst controls sitting against Playwright's 30 s per-test limit, and the attribution hole of ADR 0029. **A milestone with CI rounds is run by keeping one implementer alive**: it commits and stops, you push and hand the run back with `SendMessage`, its context intact — M10 took ten rounds that way and a fresh agent per round would have re-derived the evidence each time. M09-sized milestones do not fit one implementer: M09 needed three and M09b's seven steps were cut 1–3 / 4–5 / 6–7 and needed two fix rounds on top, so **cut a renderer or cross-thread brief into step ranges of about three up front**. **Get a Sonnet review agent on any renderer or cross-thread diff over about 2,500 lines** — not 3,000: M09b's was 2,670 when the review found two coverage gaps and led to a real shader bug, none of them visible to a green suite. Brief that reviewer to hunt for *tests that cannot fail*, not just for defects: M09b's whole probe grid sat at texel centres, the one point where a correct and an inverted seam formula agree, and its jitter amplitude was chosen to sit under every probe tolerance. **Verify a Tyler-facing command literally yourself** rather than accepting the implementer's substitute: M09b's reported `pnpm device:serve` port collision was a misdiagnosis (IPv6 vs IPv4) and the real command worked. **When you cut a brief into ranges, name the seam *fields*, not just the files:** M11's `InputEventTs.button`/modifiers and idle-mouse `hover` fell between two of the orchestrator's own delegation prompts — range 1–3 was never asked for them and range 4–5 was told to consume `pointers.ts` unchanged — so a pinned Seams contract shipped hardcoded to zero until a third range was told to fix it. **When CI is red after a green local gate, the runner is usually right and the repo is usually wrong**: M10's Rust-pin and toolchain findings were real defects latent on any machine whose defaults differ, not CI quirks — fix the defect, never the workflow (an added `rustup target add` would have hidden it). **And when the fix is genuinely a time limit, say why it is not a mask**: CI already scales suite budgets by 1000 under ADR 0020 §10, so raising the `gc` project's per-test timeout there was applying an approved policy where it had been missed — measured, scoped to CI, local left at 30 s. **Expect implementers to end turns with background work running** despite being told not to (three did across M09 and M11): check `pgrep` before touching the tree rather than trusting the instruction. **And when a report offers you options, notice how many of them are ways of not fixing the thing** — M11's steps 6–8 offered "accept the flakiness", "investigate", or "demote the test to `@slow`"; two of the three were exits, and the one real option took four rounds and found a V8 JIT artefact underneath. The `browser` wire stays armed at 20 s quiet (`docs/plan/deferred-ledger.md`); new zero-GC pages get `@slow` bursts automatically. When a milestone adds cross-thread tests: repeat `browser` about 30 times plain and 30 under `--load 10` (Rules). **This is not a formality:** M09b's implementers called a `viewport: resize renders same frame` failure an environment hiccup, and the loops measured it at 2 failures in 16 quiet runs — a fast-tier flake that would have made M10's CI red on arrival. Note it failed *quiet* and survived `--load 10`, because `ResizeObserver` delivery is scheduled by the rendering pipeline rather than by JS task order: a quiet-only failure is a real ordering signal, not an absence of one, so run both tiers and read the asymmetry. Run the machine-hygiene commands (Rules) before gating. Run `pnpm test && pnpm lint` again after your own record edits, before every `done` commit. The next marker is M16 (`vertical-slice-complete`).
- **Blockers:** none.

**Keep the State line from growing without bound.** It is a running summary, not a log: when a milestone is two or three milestones old and its brief's Deviations hold the detail, compress its entry to the names a future session needs to grep for. M04–M08b were compressed this way at M11's `done`.

Overwrite this block (never append to it) and commit it twice per milestone: before delegating (`M<NN> start`; Milestone reads "M<NN> in flight, base `<sha>`", the sha of the commit before it) and after accepting (`M<NN> done: …`; Milestone names the next row). Leave the rest of this file intact.

## Your job

You are the **orchestrator**; run this session on Fable or Opus. Land as many milestones as fit, in `PLAN.md` order, one after another. You pick, brief, check and record. You do not write the milestone's code and you do not read its reading list: a Sonnet sub-agent does (ADR 0025).

## The loop

Once per session:

1. **Read** `docs/process.md` ("Rules for every session"), `PLAN.md` ("How milestones work" and the table) and `.claude/agents/milestone-implementer.md` (what your implementer already knows, so your prompts can stay short). Nothing else up front.
2. **Check the ground.** You are on `main`, and `pnpm test && pnpm lint` is green. If Status says a milestone is *in flight*: `git log <base>..HEAD` step subjects plus the brief's Deviations say how far it got. A dirty tree is handed to a fresh implementer as it is (never stash or reset); continue at step 4 with "continue from step k". A dirty tree with nothing in flight is not yours to clean up: ask Tyler.

Per milestone:

3. **Pick** the first unticked `PLAN.md` row whose **After** rows are ticked. A row that needs Tyler first (a push, a deploy, a login, an answer) is skipped while another row is ready. Read its brief, and only the brief. Overwrite Status and commit `M<NN> start`.
4. **Delegate** to `milestone-implementer`. The prompt is the brief path, a step range if you are cutting, and what it must know from earlier Deviations that the brief does not say. If that agent type is not registered, use `general-purpose` with `model: sonnet` and tell it to read `.claude/agents/milestone-implementer.md` first and follow it. Foreground, one at a time: there is one working tree, one cargo target and one set of test ports, so no other agent builds or tests meanwhile. Every other delegation (research, review, ADR draft, doc sweep) is `general-purpose` with `model: sonnet`, briefed with files (ADR 0021 §3). Two exceptions are written into their briefs: M39 and M39b are run by you, because they are delegation and judgement and a sub-agent cannot launch sub-agents; in M10 you push for the implementer, which never pushes.
5. **Accept.** The report is a claim; check the repo:
   - `pnpm gate <base>`: tree clean, files changed, existing goldens modified or deleted, skip/ignore/only/todo markers added, diff size.
   - `pnpm test && pnpm lint`, run by you. For slow-tier commands accept the implementer's pasted result line; re-run them yourself only at a tag milestone.
   - The changed files sit inside the brief's "Files touched"; every name under "Tests added" exists (`grep`); every **Provides** name exists (`grep -n`; do not read the files). A diff over roughly 3,000 lines gets a Sonnet review agent, not your read.
   - A criterion that says "by hand" or "in this session" needs automated evidence (`playwright-cli`, a Node `fetch`, a pasted command line with its output) or it stays unticked and goes under "Criteria awaiting Tyler" in `docs/plan/questions-for-tyler.md`. Never tick on a claim. Like a device check, a criterion that only Tyler can verify does not block: when it is the only kind left unticked, the milestone is still recorded as done.
   - **Decisions needed** in the report: a technical one is yours (decide, record it per the Deviations rule below, send the implementer back with the answer); one that is Tyler's goes into `questions-for-tyler.md` with a recommended default, and the implementer continues on the default.

   Failures go back to the same implementer (its context is intact) or, if it is unavailable or near its limit, to a fresh one after you write the failures under "Open gate failures" in the brief's Deviations. Two rounds at most; then one implementer with `model: opus`; then split the milestone or record a blocker. Your own fixes: under about 20 lines, in one file.
6. **Record.** Tick the brief's exit-criteria boxes and set its `Status:` to done; tick `PLAN.md`. A changed decision gets an ADR drafted by a Sonnet agent with the `write-adr` skill and reviewed by you. Deviations that touch later briefs: a Sonnet agent fixes those briefs from the Deviations text and you review `git diff --stat`. `questions-for-tyler.md`, coverage rows and `deferred-ledger.md` are yours (or a Sonnet doc agent's). Overwrite Status; commit `M<NN> done: …`. On a marker row (`PLAN.md` names the tag): `git tag -a <tag> -m "<what is true now>"`.
7. **Continue** at step 3. **Stop** at a milestone boundary when context use passes roughly 50 % (if you cannot see it: after six milestones, a milestone that needed a fix round counting double), or when every ready row needs Tyler. On stopping tell Tyler: milestones landed, tags made, device checks ready (`docs/plan/device-checks.md`; they never block the next milestone), criteria awaiting him, open questions, and that `main` is ready to push.

## Rules

- **Oversee, don't build.** If you are about to read a reading-list file or write milestone code, delegate instead.
- **Goldens, tests and checkboxes are evidence.** A changed existing golden or a weakened test is your decision, never the implementer's. You are the only writer of exit-criteria boxes, `PLAN.md` and this file.
- **Trunk only.** Everything is committed on `main`; no branches. `main` may be red between step commits and is green at every `M<NN> done:` commit (last green: `git log --grep ' done:' -1`). **Push only `done` commits** (M10's CI runs on every push); pushing is not allowlisted, so it prompts Tyler.
- **Deviations are written down, not drifted into.** If the brief is wrong, too big, or contradicted by what the implementer finds: a small correction goes in the brief's **Deviations** section (and later briefs it affects are fixed); a changed decision gets a new ADR that supersedes or amends the old one (never rewrite an accepted ADR) plus a line under "Plan-level decisions" in `PLAN.md`; a milestone that does not fit one implementer is split (`<NN>b-<slug>.md`, new row in `PLAN.md`, written by you) rather than overrun.
- **Seams are contracts.** Names under a brief's **Provides** are consumed by later briefs. If one must be renamed, grep `docs/plan/` and update every consumer in the same commit.
- **Context budget.** Yours is spent per milestone on one brief, one report and a few command lines. Stop at a milestone boundary (loop step 7), never in the middle of a gate.
- **Questions for Tyler** are batched in `docs/plan/questions-for-tyler.md` with a recommended default; proceed on the default and mark what depends on it. Scope, taste, cost, and anything that changes a Requirements section in `docs/spec/` are Tyler's; technical choices are yours, recorded in an ADR or the brief.
- **Budgets** live in `PRE-PLAN.md` §7 (by ADR) and, once M04 lands, in `packages/engine/budgets.json` (what tests assert). A milestone that cannot meet a budget says so in Deviations and opens the question; it does not quietly raise the number.
- **A report is a claim, and so is "flaky under load".** M06's livelock and M06b's per-pass boxing and lost wake were each first explained away as scheduling noise. When a milestone adds cross-thread or timing-sensitive tests, repeat its suite yourself before accepting: `node scripts/repeat.mjs <suite> <runs> [--load <n>]` (per-run kill timeout, self-terminating burners; a Bash call caps at 10 minutes, so `browser` in batches of 15–20, in the background), about 30 runs as the machine is and 30 under `--load 10`, 0 failures and 0 hangs. A hang that outlives the runner's timeout with one thread at 100 % CPU is a synchronous spin, not contention.
- **Machine hygiene before any gate and after any report (or missing report).** Suite budgets are wall-clock and the machine is shared: `uptime` (at a 1-minute load near 20 every suite fails on timeouts only), `pgrep -x yes | wc -l` (orphaned load generators: must be 0), `lsof -ti tcp:4517` (an orphaned `vite preview`, which Playwright would reuse: kill it), `pgrep -fl "vitest|scripts/test.mjs|scripts/gc.mjs"`. A `playwright-cli` daemon may belong to another session: leave it. Say "loops run in the foreground, bounded, with a per-run kill timeout; no background load generators" in every delegation that involves loops: implementers wait on background tasks, get lost and leave orphans.
- **Look for masks in every fix round, before reading anything else:** diff `budgets.json` numbers, grep the diff for timeouts, retries, new constants and reduced workloads. Under a red gate implementers have reached for a longer timeout, a widened budget with "headroom", a halved tick rate and a production busy-wait.
- **Quantify before hypothesising.** `bytes ÷ wakes ≈ 14.7 B` named one HeapNumber per pass, and a fixed 136 B delta named a one-off, before anyone opened the code. Ask for per-function attribution first and make the implementer follow it; state your own mechanism guess as a guess.
- **The commit gate lints the whole tree,** so dirt left by a lost implementer blocks your own commits. Hand it to the next implementer uncommitted (loop step 2); never reset it.
- **Verify, don't recall.** Browser, tooling and Claude Code behaviour change quickly; have current docs checked before relying on an API or flag.

## When Phase 3 ends

`docs/plan/39-acceptance.md` checks the Phase 3 exit in `docs/process.md` (the reference game works in single-player and multiplayer; the full test suite passes within budget); the last milestone, `docs/plan/39b-phase-4-handoff.md`, rewrites this file for Phase 4 and ends with the `phase-3-complete` tag.
