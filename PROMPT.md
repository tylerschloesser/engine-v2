# PROMPT: Phase 3 (Execute)

## Status

- **Phase:** 3 of 4. (Updated 2026-09-25.)
- **Milestone:** **M25 next** (`docs/plan/25-prediction-core.md`, After 21b, 16b: `Predicting` overlay, pending queue, taint rule, provisional ids, `entities_in`). Its brief carries a note from M21b (the undo journal is host-only). Read the brief for whether to cut. **M24c landed** (engine-edit rebuild 210-230 s → ~147 s by consolidating `crates/engine`'s integration tests; the rest is macOS's per-binary first-launch check, fixed out of repo by Tyler enabling Developer Tools for his terminal; ledger row). **This session landed M21b, M22, M22b, M23, M24, M24b and M24c.** **M21b** (`docs/plan/21b-timers-wakeups-and-tickcx.md`): timer wheel, wake queue with auto-wake on every put outside `G::tick`, active lists + `Registry::system`, final `TickCx`, `machines` tick rule, **ADR 0037 adopts the undo journal** (release rolls back, debug/test panic). `Store::encode` gained three sections, so every full-store hash moved and was re-blessed by command: `puts_idle_100` `4e60d654ed1d2cba`, `puts_script_a` `0a7cc2623a83a03e`, `puts-connected` `3a392e50dba8f378`, `machines` `58cf4dd2cba3ea96` / full-world `a813319e1e3ca417`. Its review agent found a release-mode rollback defect (timer + active slot lost on a rejected despawn). **M22** (`docs/plan/22-persistence-log-and-snapshots.md`, cut 1-3 / 4-6, three fix rounds): `engine::persist` containers, `testing::{replay, heavy}`, fixture `persist`, ABI exports `sim_segment_header`/`sim_snapshot_begin`/`sim_snapshot_next`/`sim_dirty` + a real `sim_seal_frame`, `memoryStorage`/`runStorageConformance`/`worldKeys`, `Persistence` wired into `SimHost`, `logBytesPerAction` 12. **ADR 0038 amends 0005's formats** (`total_len`, length-prefixed action payloads, `log_ref_tick`). The fix rounds found a real format defect (replay resumed from a snapshot after an idle gap used the wrong tick reference), and the review agent found `skip_kind_decodes_as_noop` could not fail. **M22b** (`docs/plan/22b-persistence-load-and-fs.md`, cut 1-3 / 4-5, one fix round): restore/replay ABI (`ABI_VERSION` 17), `Persistence.open`/`loadLatest` (static), `crashClone` crash matrix over every byte cut, segment rolling (`PersistenceOptions.segmentRollBytes`), pruning, async `SimHost.pause()`/`stop()`, `fsStorage` (`engine/server/node`), `engine/test` `replayWorld`/`runHeavy` with a Bun leg. Its review found four tests that could not fail (un-awaited `flush()`, a one-hop exports-map scan, a named test never built, an unreached pool-exhaustion branch). Notes for M24 (single-pass replay, the `Skip` scan pass is M24's) and M27 (a loaded `Sim` has no connections) are in those briefs. **M23** (`docs/plan/23-persistence-opfs-and-lifecycle.md`, cut 1-2 / 3-4 / 5-7 plus a gate fix round): `opfsStorage` (`move()` 2-arg rename in all three engines, no slot files), sim worker start-up (Web Lock → OPFS → `Persistence.open`), `world-busy`/`load-failed` start codes, `durable: false` fallback, `client.onStorage`, `attachHostLifecycle` (hidden → async pause + snapshot), `hostWorkerLock` serializing every park of the sim worker, archive + `exportWorld`/`importWorld`/`deleteWorld`, `world.html`, `opfs-latency.html`, **ADR 0039** (the snapshot is a budgeted event: 2,828 B measured, `snapshotEventBytes` 3,600, asserted as its own delta). **`Shell.runAsync` never worked from inside a tick pass (M06b seam, first caller here): a continuously ticking session landed zero OPFS snapshots until M23 fixed it.** The sim worker's `postMessage` allowlist is now a real test (`worker/protocol.test.ts`), not M06b's one-time grep. `CB_FORCE_SNAPSHOT_REQ` used the last free global control word. **M24** (`docs/plan/24-recovery-and-migration.md`, cut 1-3 / 4, two fix rounds): `panicky` fixture, `Progress` cursor (`RegionId` **11**), `Skip` scan pass in every replay path (Rust, `replayWorld`/`runHeavy`, native `testing::replay`/`heavy`), `recovery.ts` (recurs rule from the dead instance's cursor, loop guard 3 per 1,200 ticks), `sim_reattach`, `sim_fault_ack` for Admit-phase traps, `SimHost.onRecovered`/`onFatal`/`recover()`, `ABI_VERSION` 20. The orchestrator found `recovery_loop_guard` derived its counts from the constants it guards (a changed limit still passed); now pinned. **M24b** (`docs/plan/24b-upgrade-and-migration.md`, cut 1-3 / 4-5, three fix rounds): `Identity::compare` (any schema difference, either direction, goes to `migrate`), `Rescale`/`RescaleTicks`, `OldStore`/`Migrating` (ids preserved, `spawn_no_wake`), fixtures `fx-migrate-v1`/`v2`/`v2-hz30`, `sim_upgrade_*`, `sim_identity_compare` (the decision matrix exists once, in Rust), `'save-incompatible'`, `ABI_VERSION` 23. Gate and review found the genesis-only load path skipping the identity check, then a corrupt header passing it and a TS copy of the matrix; all fixed. **After any engine-crate change `pnpm test` spends ~170 s building** (it shows under `fx-puts`' bindings step only because that step runs first and builds every workspace test binary with `--workspace`, which M17d made deliberate; ledger row has the corrected diagnosis). M24c attributed and cut it; the rest is macOS's first-launch check (ledger row). **Every milestone with a review agent has now found a test that could not fail or a defect a green suite hid** (M20b, M21, M21b, M22, M22b, M23, M24, M24b): keep briefing reviewers for exactly that, and brief implementers to prove replay/format tests on logs a real host writes. **A test that imports the constant it guards cannot catch that constant changing: pin the specified numbers.** **Implementers' long Bash calls auto-background after 120 s unless they pass `timeout: 600000`**: say so in every delegation, or "foreground only" silently becomes background. **Push every `done` commit** (Tyler authorised pushing without asking, M11 session: "Just push. No approval necessary").



- **State:** M01–M14 are done and on `main`, **M13b included** (it is numbered out of order: written at M14's gate, landed after M14) (tag `harness-complete` at M04's `done` commit); `pnpm test && pnpm lint` is green — **current ground, re-verified by `pnpm test && pnpm lint` at M19b's `done` gate and machine-checked by `pnpm handoff` against the `handoff:ground` marker below: `rust` 551, `unit` 252, `wasm` 147, `browser` 201 at 32-33 s of 48 s quiet, up to 65 s under `--load 10` (re-verified at M24b's `done` gate; ~15 s of headroom left, so keep new browser tests few and short), 36-39 s under `--load 10` (a WARN, not a fail; re-verified at M21's `done` gate) (ADR 0033; 32-33 s under `--load 10`, so every milestone that adds browser tests must keep them few and short), lint clean**. <!-- handoff:ground rust=551 unit=252 wasm=147 browser=201 --> (Historic, for the M13b-era reasoning below: `rust` 235, `unit` 154, `wasm` 41, `browser` 98; **`browser` is at 20 s of 25 s quiet — the armed threshold, reached at M13b**, one milestone earlier than the M15b forecast, because `gc-sim-paced` added three tests. It was not demoted: 20 s was the trigger to decide, not a failure, and M13b was a defect fix that could not be deferred. **That era's standing instruction — "the next milestone that adds browser tests must take 0020 §4's next rung" — has since been overtaken by measurement and must not be reused: see the Milestone line, the ladder is exhausted** (`deferred-ledger.md`).) Phase 3 runs as one orchestrating session with Sonnet implementers on `main` (ADR 0025). **M04–M11 are settled; each brief's Deviations hold their exact seam shapes, so only the names are here.** M04: the zero-allocation instrument (`tests/browser/gc/`, `zeroGcSuite`, page `gc-loop` with five permanent negative controls), `budgets.json` + `tests/support/budgets.ts`, `installGcPage`, `pnpm gc <software|flat|reliability>`, the `gc-test` skill. M05: `engine::codec` (`Codec`, canonical NaN, `decode_canonical`), `engine::bytes` (`ByteSink`, `ByteReader`, the one varint), `StateHash` / `hash_value` / `mix64`, byte goldens (`assert_golden_bytes!`, `pnpm golden:bytes`). M06: `src/sab/` (SPSC ring, seqlock, triple buffer, control block, `SabSet` / `createSabSet`, `RING_DEFAULTS`) and `src/camera/{state,block}.ts`. M06b: `createClient` / `checkSupport` / `EngineStartError`, `engine/worker` `run()`, the `yield` protocol (`runBlockingLoop`, `Shell.observeWake()`), the `frame` export, zero-GC pages `topology` and `echo`; its Deviations end with "Notes for later briefs". M07: `engine::world` (`Tile`, coords, `ChunkDims`, `Registry`, `PristineSource`, `TerrainStore` with canonical overlays over an invisible LRU dense cache), `engine::testing::assert_cache_invisible`. M08: `engine::worldgen` (`Worldgen`, `hash2`, `Pristine<W>`, `worldgen_fingerprint`, `WorldgenStamp`, `GenCore`), `engine::noise`, `gen_chunk`, `fx-worldgen` with a six-runtime golden, slow-tier `worldgen-bench`. M08b: `engine::gen_queue` / `engine::view` / `client::TerrainFeed`, `gen_take` / `gen_deliver` / `client_gen_stats` / `client_chunk_hash`, the gen worker body and client pump over `genRequest` / `genResult`, the test-gated parked `test-call` channel (`callParked`), pages `gen` and `gc-gen`. M09 + M09b landed the terrain data path and its art (`client::{texel,upload}`, `upload_stage`, `uploadRing.slotBytes` 4120; `src/render/{device,art,terrain,upload,mips,viewport}.ts`, `terrain.wgsl` via `scripts/embed-wgsl.mjs`, `frame-loop.ts`, `createRealFrameLoop`, `ClientOptions.render`, `fixtures/terrain`, `renderTo`/`readPixels`/`setViewport`/`setVisibility` in `engine/test`, pages `terrain`, `terrain-client`, `gc-terrain`, `device.html` + HUD); **its standing reservation about making recompilation synchronous is superseded by 0028 and must not be acted on.** **Carry forward:** magnified sampling must anchor on `floor(texel + 0.5)`, not `floor(texel)` — inverted from M09 until M09b's review found it, invisible because every probe sat exactly at a texel centre where both formulas agree; **M17b reuses this sampling path for sprites** (its brief carries the warning). M11 landed the engine-owned camera (`camera/{transform,camera}.ts`), the fixed-slot listeners (`input/{pointers,keys,wheel}.ts`: **listeners record, rAF integrates**), semantic events and `client.input`, the `inputRing` → `on_input` → Rust `InputQueue` path, `moveTo`/constraints/`localStorage` persistence, the zero-GC page `input`, and `device.html`'s gestures and `?probe=memory`; four fix rounds, the last two on an Opus implementer whose reading of the raw `profile.samples` found **a one-off V8 JIT code-installation burst billed to whichever JS frame happens to be executing** (seven were observed, so no name-based exclusion could work). **ADR 0028** (superseding 0027, amending 0016 §3 step 7) makes assertion B take the lower of two consecutive 600-frame windows and re-derived every page's `main` budget downward; **the strict worker figure stayed 8 throughout — what is counted changed, never what is allowed.** **M10 landed CI** (`.github/workflows/ci.yml`: one `ubuntu-latest` job, push-to-`main` + PR, `cancel-in-progress`, actions pinned by SHA; `pnpm lint` + both tiers under `CI=true ENGINE_GPU=swiftshader GC_MODE=software`; `--timings-json`, timings recorded never gating via `--budget-scale 1000`). **Spike B closed positively:** `channel: 'chromium-headless-shell'` gives a fallback SwiftShader adapter and every M09 readback scene passed **with no golden mismatch against the Metal goldens**, so no per-adapter-class golden exists; x86-64 determinism (0002) and recursive `fs.watch` on Linux are **closed**. Its real product was **ADR 0029**: `GC_MODE=software` ran 0016 caveat b's attribution mechanism for the first time ever and it did not work — control hooks fired as *siblings* of their roots, so every worker control and `main`'s control on all five pages attributed 0. Hooks now **nest inside the root** (never widen a root to reach a control), and software mode **attributes only `main`; every other isolate uses raw bytes in both modes**. **The nested control is a live inlining detector: a control that stops tripping is a signal about the instrument, never something to fix by widening a root or relaxing a budget.** Also live: `@webkit-gpu` runs everywhere but Linux (WebKitGTK ships no WebGPU — asserted against the platform, never discovered from a missing `navigator.gpu`); the `gc` project's per-test timeout is 90 s under `ENGINE_GPU=swiftshader` only; a temp fixture crate copies `rust-toolchain.toml` so it stops compiling with the runner's default Rust. Runner baseline: fast `browser` 103 s/25 s, full two-tier cold job ~5.5 min, and `ubuntu-latest`'s CPU varies run to run, so that alone is not a regression signal. **Open on the ledger, not fixed:** one `wasm` runner exit-1 with a passing report (run 35619437805), never reproduced; and whether `main` needs attribution at all under 600 frames (M36b). **M12 + M12b landed the game API, data half then execution half** (exact shapes in their Deviations): the **full** `Game` trait of 0003 with every associated item declared up front (no-op shells for `TickCx`/`FrameCx`/`FrameView`/`DrawList`/`PresenceTable`/`OldStore`/`SaveIncompatible`, because associated type defaults are unstable and a later `type Ui` would break every fixture), `Delta<G>` with the engine-only `Roster` variant (0024 §8), `Store<G>` over M07's `TerrainStore` with `apply` as its only mutator, canonical `encode`/`decode` + `StateHash`, `SimRng` (PCG32-XSH-RR) and fixture `fx-puts`; then `WorldRead`/`WorldWrite` (object-safe, upcasting test), `Authority<G>` (a write = `Store::apply` + one scoped `(Scopes, Delta)` on a reused `ChangeLog`), `View`, a minimal index-based `TickCx`, `TickRate::{millis, secs, dt}`, `Record`/`Outcome`/`EngineReject`, `Sim<G>` running records in 0004 order, and goldens `puts_idle_100` = `195e71ef0defbf7a` / `puts_script_a` = `7bdddfc9c749b1fb` (**M16 makes the second `.wasm`-authoritative — do not re-bless by hand**). `Store` holds no `Tick`/`SimRng` (they are `Authority`'s); `ts-rs` is a real dependency of `engine`/`fx-terrain`/`fx-puts` (pre-approved, 0017 §7); build step **`doctests`** in `scripts/suites.mjs` because nextest runs no doc tests. **Three standing seam notes:** `Delta`'s 7th variant `Ack` bypasses the `ChangeLog` and never reaches the wire; `TickRate::hz()` the getter is **`hz_value()`** (0006's `hz` is the const constructor); and **`WorldRead::entity_at` is `Ok(None)` until M21**, so `fx-puts`'s `Bump`/`Remove` reject `NotFound` **by design** — not a bug. **M13 landed the sim host and tick loop**, cut 1–3 / 4–6 up front: `host::Host<G>`, `GameInstance<G>` dispatching on `Role`, `host::warm` (nearest-first, hash-invisible), **`ABI_VERSION` 6 → 8** (`sim_genesis`, `sim_seal_frame`, `sim_warm_one`, `tick_hz`), `SimHost` in `src/server.ts` (no `node:`/DOM imports) with `start`/`stop`/`pause`/`resume`/`stepTick`/`hash`/`counters`/`logSink`, `AtomicsTimer` over `runBlockingLoop`, the `sim` worker kind with a new `CB_SIM_STEP_REQ` word, `engine/test`'s `stepTick`/`worldHash`/`simCounters`, and the `gc-sim` page. **`puts_idle_100` is `.wasm`-authoritative** in `fixtures/puts/golden/golden.json` and the native Rust test reads the *same* file, so native-vs-wasm equality is checked continuously rather than once at bless time. **Counters are not all live:** `ticksRun` is; `ticksDropped`/`tickOverruns` only in unit tests; `chunksWarmed` is unreachable until M15's `set_view`; **`genOnMiss` is permanently 0** (no export) — do not read the last three as coverage, and **15b's gate is where `chunksWarmed` becomes real** (M15 wired `Warm::set_view` from `build_frame`, but the counter is a TS counter reached through the ABI, and `sim_admit`/`sim_build_frame` stay at `Instance`'s `Status::Unsupported` defaults until 15b). `cacheChunks` is parsed but inert by design. **M14 landed the wire codec** in one implementer plus one fix round: `engine::wire` (`mod.rs` holding `WireError`/`zigzag32`/`unzigzag32`/`MsgType`/`SectionId`/`FrameHeader`/`SectionWriter`/`FrameWriter`/`FrameReader`, plus `coordlist`, `overlay_runs`, `snapshot`, `deltas`, `results`, `global`, `uplink`), 10 `wire_*` goldens, `tests/no_alloc_wire.rs`, and **`crates/engine/src/wire/CLAUDE.md` as the single home of the id tables and byte layouts** — no ABI change, `rust` 191 → 235. Two accepted deviations, both load-bearing for later briefs: **`encode_chunk_snapshot` writes content only (version, overlay runs, entities), never the chunk coordinate** — `SnapshotWriter` chains the coord via `ChunkCoordListWriter`, so M31b's per-chunk desync hash is self-contained instead of depending on a neighbour's position; and **snapshot entities are anchor-chunk-only, not footprint overlap** — identical sets today because `Authority`'s scope derivation is anchor-only, and **M21 must widen both together** (its brief now says so explicitly). Additive and non-breaking: M05's `codec::encode_to`/`encode_to_with` widened to `?Sized`, `Store::entities()`, `+ Clone` on `ChunkOverlay::entries()`. **Notes handed to M15** (in its brief): `encode_chunk_snapshot` does two `O(all entities)` scans per chunk — a cost to measure under real frame building, not a defect to fix blind; nothing in `wire/` matches on `Delta`, so the `Ack` arm is M15's to handle deliberately; `UplinkReader` returns raw `(seq, &[u8])` action bytes and carries no `G`. **Reading CI at M14's gate found three defects that the green local gate could not**, none of them M14's, and they are the reason M13b exists. (1) **`gc: flat transport parity` was intermittently red**: attribution showed the whole 8-byte discrepancy was `armedLoop@harness-worker.ts` posting fresh `{type:'armed'}`/`{type:'parked'}` literals once per measured window — present and stable in *both* windows, so 0028's lower-of-two filter could never remove it, and sized 32 or 40 B depending on the session. Fixed by hoisting both to module constants (`d602f44`); green since. (2) **Every `zeroGcSuite` failure message now carries `windowByFn`** (`2024709`, `tests/browser/gc/suite.ts` + `instrument.ts`) — per-isolate, per-window `byFn`. **This is what found the other two, and it is the technique to reach for first:** ~130 local runs across two rounds reproduced neither flake on macOS/arm64, and one enriched failure message named the function on its first red CI run. (3) **`sim neg object main` / `sim neg burst main` / `sim neg burst sim` are intermittently red on a real M13 defect**: `SimHost.runOneTickTimed` reads `services.clock.now()` twice per tick, and `performance.now()`'s fractional double boxes a `HeapNumber` in the interpreter tier — **11.92 B per read, 23.84 B/tick, against a strict worker budget of 8, so reading once is not a fix**, and `Math.trunc()` does not help because the box happens at the native-call return boundary. Reproduce on demand with `--js-flags=--no-opt --no-sparkplug` on the `gc` project, then `pnpm gc -t "sim clean"`. Intermittent because a busy sibling control plausibly starves `sim`'s TurboFan job so the window runs interpreted — the trigger is a hypothesis, the defect is not. **`onFire`, the production pacing path, has the same read and no zero-GC page exercises it** (`gc-sim` sets `test.flags.gcHook`, so `simHost.start()` never runs), which is why M13b builds that page as step 1. The M06b **sibling-isolate nudge** was our working theory for two rounds and is *not* this: it stays open and documented in the `gc-test` skill, and the numbers here are fully explained without it. **M13b landed the fix** (`docs/plan/13b-tick-timing-allocation.md`, ADR `0030`, written at M14's gate, one implementer, no fix rounds; **its Deviations hold the measured numbers, the seam shapes and the recorded `client.ts` `now()` finding**): `SimHost` reads the clock only once every `RESYNC_TICKS` = 8 ticks, integer-only arithmetic between resyncs; `AtomicsTimer` no longer takes a clock at all (`timeoutMs()` returns the fixed armed interval, `poll()` fires unconditionally). New page **`gc-sim-paced`** (`TestFlags.pace`, a `worker/sim.ts` gate) is the **first zero-GC page ever to exercise the production `onFire` path** — every earlier page drives ticks through `CB_SIM_STEP_REQ`, so the real pacing loop had never been measured, which is why the defect survived M13's gate. `gc-sim` keeps its original test-driven coverage; both pages are green. Measured: `sim` 3.73–3.81 B/frame against the strict 8, `resync@` the only entry in `byFn` (1752–1800 B over 600 ticks), `poll`/`timeoutMs`/`onFire`/`now`/`stepTick` **gone, not shrunk**, and the same under forced `--no-opt --no-sparkplug`. No ABI change (JS-side only). Six `server.test.ts` tests were rewritten for the new semantics — **orchestrator-approved, and they gained assertions rather than losing them**; a new `tests/wasm/puts.test.ts` test drives the real `.wasm` and asserts exact overrun/drop counts plus a clean on-schedule run, so a constant-wired counter fails. **`sim-paced`'s software `main` budget is 14, derived as `ceil(11.98) + 2` rather than the customary `+8`** — and the reason is a trap worth remembering: the orchestrator told the implementer to derive from the forced-interpreter worst case, and taken literally that gives a budget wide enough to swallow the `object` control's own stable 16 B/frame, which measured **0/8 trips**. The implementer caught it and narrowed the margin instead (verified 5/5 on both tiers: clean passes, control still trips). **`main`'s hardware figure stayed at 30 for the same reason** rather than rising to cover the interpreter worst case. On a page whose `main` sits near the control's own magnitude the usual `+8` margin does not fit, and a budget that makes a control stop tripping is ADR 0029's exact failure mode. **Recorded, not fixed:** `client.ts`'s `now()` costs ~11.94 B/frame on `main` in the interpreter tier — the same defect class M13b fixed on the sim worker, quietly absorbed by every page's `main` budget since 0028's re-derivation, and a candidate milestone of its own. **Carry to M15:** `poll()` firing unconditionally is correct only while nothing but the timer wakes a production sim worker — M15's brief now says to revisit it before wiring any external wake. Known: commits `5cbf35e` (`M03 done`) and `e6767d6` are red on one `unit` test (a `CLAUDE.md` line cap, an orchestrator's slip; fixed in M04), so do not push those two as heads. Device checks ready for Tyler: M03 determinism page, M08 worldgen ms per chunk, **M09b terrain fill rate**, **M11 boot / gestures / memory** and **M11 desktop-Safari trackpad pinch** (`docs/plan/device-checks.md`; M09b's and M11's are run in the same sitting, and M11's `&touch=1` has a known gap recorded in its own item; M09b's is the first that needs a phone and a tunnel: `pnpm device:serve --tunnel`, then `device.html?autopan=1&tiles=256&scale=2`). Open questions for Tyler (none blocking; defaults stand): `docs/plan/questions-for-tyler.md`. **Ground re-verified at M15's start on `ebea2a3`:** tree clean, `pnpm test && pnpm lint` green (rust 235, unit 154, wasm 41, browser 98 at 20 s/25 s; biome/rustfmt/clippy/tsc clean). **M15 landed the Rust core of connections** in one implementer plus three fix rounds, the last on Opus (`docs/plan/15-connection-and-subscriptions.md` Deviations hold every seam shape and measured number): `host::Host<G>::{connect, disconnect, on_uplink, tick, seal, build_frame, region_hash}` over 8 fixed connection slots, `host::subs::SubscriptionSet` (ring 1 + look-ahead, hysteresis, `CAP_CHUNKS` 128, clamps), `client::{Replica<G>, ClientCore<G>}` with validate-then-apply atomic `on_frame`, `testkit::Loopback`, 10 new `wire`-driven tests plus a `join_wilderness` byte golden, `testing::budgets` (the crate's first native reader of `budgets.json`), and `tests/module_layering.rs` pinning the crate `CLAUDE.md`'s new line that `host/` and `client/` sit outside the deterministic core. `rust` 235 -> 266. **Per-tick call order is `tick()` once, `build_frame(conn)` per connection, `seal()` once**; `PlayerId = conn + 1` (0 is reserved), not the brief's `conn`. **Three standing notes:** entity delivery is anchor-chunk-only and **M21 must widen the frame builder, `encode_chunk_snapshot` and `Authority`'s scope derivation together**; `Delta::Roster` has no producer yet, so `Global`'s roster is always empty; and `Delta::Ack` has an explicit empty arm, never a catch-all. **The gate's real work was one number.** A new panning zero-allocation test measured a flat ~95 B/tick that did not decay with run length. Two rounds explained it as `BTreeMap` node churn; **that is impossible, because `abi::arena::live_bytes()` is live bytes, allocated minus freed, so an alloc/free pair nets to zero in it** — one grep at the gate overturned both rounds. Opus then attributed the whole rate with **remainder exactly 0**, host and client measured separately: **the entire client path allocates 0**, and all growth is host-side and proportional to newly reached territory, never to time — proven by a **bounded-camera control that allocates 384 B flat at 300, 1,200 and 4,800 ticks alike**. It found a third grower nobody had anticipated: **`TerrainStore`'s cache-event queue has exactly one consumer (`client::upload`, M09's texel path) and nothing on the host path ever drains it** (80 -> 3,936 over a 4,800-tick pan), unbounded *in time* unlike the other terms. Fixed by making event recording **opt-in** (`TerrainStore::enable_cache_events`, off by default, enabled where a store is paired with an `Uploader`) rather than by bounding or dropping pushes, which would have given a client missing terrain with no signal. **Accepted and recorded, not fixed:** `Host::chunk_versions` gains a permanent ~26 B entry per distinct chunk ever touched and is never pruned — bounded by world state, not time, but it **grows faster than world state** (300 keys against 150 overlaid chunks; half belong to chunks an entity merely passed through, left pristine and empty), contra the brief's own "stored with the chunk" wording; ledger row, candidate owner M31b. Overlay growth is `hot-paths.md`'s named exception, now measured rather than asserted. **The reshaped assertions are the template for every later no-alloc page:** the bounded-camera control asserts growth(1,200 ticks) **equals** growth(300 ticks) — per-tick growth exactly zero, with no budget number in it to widen — and the panning case asserts bytes **per newly reached chunk** (83.32 measured, 88 ceiling from summed unit costs), never per tick, because a per-tick ceiling passes silently if cost per chunk doubles while the pan rate halves. Both proved failable by injection at opposite ends of the wire. Also measured for M31/M36b: a 2,000-entity, 121-chunk join costs **10.3 ms and 11,131 B**, exceeding 0010's whole 10 ms tick budget on its own. **M15b landed the in-browser connection in two implementers, cut 1-3 / 4-6** (`docs/plan/15b-ring-connection-and-replica-rendering.md` Deviations hold every seam field): **`ABI_VERSION` 8 -> 10 across the two halves** (9 at steps 1-3, `sim_connect`/`sim_disconnect` added and `sim_admit`/`sim_tick`/`sim_build_frame` made real on `Host<G>`; 10 at step 4, the client's `on_frame`/`client_poll_uplink`); `RingConnection` (`send(cls, bytes)`, `onMessage`/`onClose`, counters `downlinkRetries`/`drops`, plus `pumpRetries()` and `lastMessageLength`, neither in the brief's list); `SimHost.accept(connection) -> ConnId` 0-based with Rust adding the `PlayerId` `+1`; the client worker loop, `on_frame`/`client_poll_uplink`, and browser pages `connected`, `connected-paced`, `connected-terrain`. `rust` 267, `unit` 157, `wasm` 43, `browser` 103 at 22 s. **Steps 1-3 escalated instead of re-blessing a golden, which is the behaviour to expect and reward:** wiring the sim worker to accept unconditionally would make `fx-puts::on_player(Joined)` write state and diverge `sim_hash()` from `puts_idle_100` — **which is `.wasm`-authoritative and read by the native Rust test from the same file, the repo's one continuous native-vs-wasm equality check**. Ruling: not re-blessed; **the sim worker accepts when its SAB set carries a client link and not otherwise**, so `CB_SIM_STEP_REQ`-driven pages keep the zero-connection topology *by construction* rather than by a flag someone must remember not to set, and that is also the production behaviour; the connected scenario got its own new golden. Two per-tick `subarray` allocations were removed (one new, one inherited from M06b-era code, found at M13 and never fixed), and ADR 0030's `poll()` fix — which steps 1-3 could only prove *inert* — gained `poll_skips_a_spurious_tick_on_a_ring_wake` once step 4 made real external wakes exist. **M15b's last two deliverables were blocked by a real bug in pre-existing M07/M08b code, and M15c exists to fix it:** `Cache::evict_if_present` — the path `replace_overlay`/`clear_overlay` take — frees the slot **without calling `push_event`**, and `GenQueue::set_view` returns early on an unchanged `view.visible` **consulting no cache event at all**; both halves verified in source at the gate, not taken on report. So a chunk the client pristine-generated and then got a host snapshot for is correctly evicted and never regenerated while the camera holds still (`client_chunk_hash(0,0)` reads `NotCached` forever while its 24 neighbours stay resident). **The race is likely, not theoretical** — `TerrainFeed`'s generation scan is independent of and faster than the host round trip — and **it could not have been found earlier**: nothing before M15b's `game_instance.rs` restructuring ever had a `Replica` and a `TerrainFeed` sharing one `TerrainStore`. **M15d landed the client's clock fix** (`docs/plan/15d-client-clock-allocation.md`, written by the orchestrator at M15b's gate on Q13's default, one implementer, one fix round): **the production rAF path now reads no clock at all.** `Scheduler.requestFrame(cb: (tMs: number) => void)` was already delivering a real `DOMHighResTimeStamp` and the callback was **discarding it**, so `tick(tMs)` assigns it straight to `cameraState.frameTimeMs`; `createResyncingClock` (`clock.ts`, one real read per `RESYNC_FRAMES` = 30 calls, integer arithmetic between) remains for `test/client.ts`'s `stepFrame` and as `tick()`'s fallback when no `tMs` is in hand. **The first attempt substituted a nominal `FRAME_MS = 1000/60` and that was wrong:** `frameTimeMs` is a *timestamp*, not a delta — it feeds `ClientCore::set_camera(report, t_ms)` and `poll_uplink(t_ms)` and therefore M15's one-batch-per-interval and 1 s keep-alive pacing — so a fixed 60 fps runs that clock at ~2x real rate on a 120 Hz phone and half on 30 Hz, snapping back each resync. Results: `sim-paced`'s `main` 33.5 -> ~21.9 with **both V8 tiers now reading the same**; the **software budget re-derived downward 14 -> 9**, restoring 0016's ordinary +8 margin in place of M13b's ad hoc +2; hardware `main` held at 30; controls still trip (60/60, 32/32, 75/75); **15/15 plain and 14/15 under `--load 10`, no `sim-paced` failure in either, against 7/15 plain before**. **Two findings reported and not chased** (ledger): `ManualClock.frame()`/`.advance()` box ~12 B/frame on every `ManualClock` page, and `gc-sim`'s `main` costs 28 B/frame under forced interpreter from `stepSimTickSync`'s Atomics box — both currently absorbed by existing budgets. **M15g mechanised the staleness checks this block keeps needing** (`docs/plan/15g-handoff-checks.md`, written at Tyler's request after `PROMPT.md` went out stale a second time; one implementer, no fix rounds). **`pnpm handoff`** runs five checks, four of which also run in the `unit` suite because they are true at every commit: no milestone named as upcoming (`M<NN> next`/`in flight`/`is ready`/`on current order`) is already ticked in `PLAN.md`; `PROMPT.md`'s parens balance; every brief `PLAN.md` references exists; every ticked row's brief says `Status: done`. The fifth is the ground check, which belongs to the command rather than the suite because State's figures are legitimately stale between a milestone's start and its `done` commit: **the marker `<!-- handoff:ground rust=N unit=N wasm=N browser=N -->` sits in the State line and you must update it, with State's prose, at every `done` gate** — `pnpm handoff` then exits 0. `unit` 157 -> 178. **Honest coverage is two of the four defects that prompted it,** not the three the brief claimed: stale ground numbers and a ticked milestone named as current are both caught and were verified live by injection at the gate; the "unbalanced parens" defect was **my own misdiagnosis** — `PROMPT.md` measured 221/221 on both sides of the commit that supposedly fixed it, and what I had actually measured was an arbitrary 1,450-character window that cut off mid-sentence. **The parens check stays as a cheap always-true invariant and must not be cited as having caught anything.** The fourth defect, prose calling the previous session "this session", is out of reach; do not build heuristics for it. **M15f closed the `gc-sim` negative-control item that had been open since M06b, and the standing explanation for it was wrong** (`docs/plan/15f-step-sim-tick-sync-allocation.md`, written by the orchestrator after reading CI on M15c's push; one implementer, no fix rounds). CI's slow tier (`pnpm test:slow` under `GC_MODE=software`, which CI runs too) failed `connected-terrain neg burst sim @slow` (`main` 108.5 against 89) and `sim neg burst sim @slow` (`main` 28 against 0). **In both the control's own isolate tripped correctly** — 80,407 and 40,074 B/frame — and **`main` is what failed, gaining exactly +28.0 B/frame whenever a burst ran on the `sim` isolate.** Attribution named one frame with remainder zero: `stepSimTickSync@client-...js:242`, **16,800 B over 600 frames = 28.0 B/frame**, called once per frame by the page. The cause was `h.workers.some((w) => w.kind === 'sim')` — **a fresh closure per call in harness code**, replaced with a plain indexed loop, the discipline `allEqual`/`allResumed` a few lines below already carried and documented. **No budget moved: `budgets.json` has an empty diff** and both previously-violated budgets pass unchanged, which is the point — the failure presented as "two budgets are too tight" and the truth was "the instrument allocates". **The M06b sibling-isolate nudge is retired as the explanation for this number and must not be invoked for it again:** the sibling burst never caused the allocation, it only starved TurboFan so the window ran interpreted and made it visible. **That is also the tool to keep:** adding `--no-opt --no-sparkplug` to the `gc` project's `--js-flags` in `playwright.config.ts` turns this class of intermittent, CI-only red into a deterministic 15 s local command — it reproduced at 108.52 and 28 against CI's 108.5 and 28. Revert that edit; it is a diagnostic, never a commit. **M15c landed the cache-invalidation fix and put overlay terrain on screen** (`docs/plan/15c-terrain-visibility-and-cache-invalidation.md`, two implementers cut 1-2 / 3-5, one fix round each; its Deviations hold every seam shape, the budget derivation and both gate corrections). `Cache::evict_if_present` now reports `CacheEvent::Evicted`, and `GenQueue::set_view` re-scans when `TerrainStore::cache_invalidation_seq()` moved even though `view.visible` did not. **The counter is a peek, never a drain, and that shape is load-bearing:** `game_instance.rs`'s `frame()` calls `feed.on_frame` before `uploader.on_frame` against the same store, so a second `drain_cache_events` caller would starve `Uploader::on_frame`'s own `changed` check of exactly the event that made it necessary. **`invalidation_seq` bumps only inside `evict_if_present`, never on LRU capacity eviction, and the narrowing is the whole milestone's sharpest lesson:** the first version bumped on every `Evicted` and closed a feedback loop — rescan -> enqueue -> materialize -> LRU evict -> bump -> rescan — that livelocked the client (498 re-scans per 500 frames, `pending` stuck at 78, `__terrainClient.idle: never reached a quiet steady state`, three `input` zero-GC pages over budget, and the whole `browser` suite 21 s -> 32 s). **A green targeted Rust run proved none of it**; the full gate is what found it, and `set_view_reaches_quiescence_under_lru_capacity_churn` is the regression test that guards it (I re-ran its failability myself: re-widen the trigger and `pending` is 78 against 0). `rust` 267 -> 275, `browser` 103 -> 110 at 23 s of 25 s. **A third fix round came from CI, not from the gate:** the new page shipped `"software": null` in `budgets.json` and `GC_MODE=software` is what CI runs — `gc/instrument.ts:161` **throws** on a null software block rather than skipping the page, and `connected-terrain` was the only null in the file. Three consecutive green local gates could not see it because they all run hardware mode. Derived at 89 (`ceil(80.52) + 8`, clean 80.50-80.52 over 8 runs), with ADR 0029's failure mode checked rather than assumed: the `object` control reads 96.50 (clean + 16.0) and still trips 8/8 at 89, so the budget separates on both sides. **Before believing the local software-mode pass I re-injected the null as a control and got CI's exact error** — a green `GC_MODE=software` run proves nothing if the env never reached the child process. **Two gate corrections worth carrying:** (1) the readback's helper originally nudged the camera and its doc comment asserted a pre-existing production bug — that a chunk finishing generation raises no event, so a static camera never rescans. **False:** `insert_pristine` (`world/terrain.rs:124`), the path `TerrainFeed::deliver` takes, pushes `CacheEvent::Loaded`, and `Uploader::on_frame` sets `changed = true` on any drained event. Isolating the variable took one coordinate change: with the nudge, passes; **with both calls at the same camera position, passes**; with no extra frames, fails. `__advance` runs exactly one `stepFrame`, so the mechanism is frame count against an async gen round trip. Renamed `pumpFrames`, camera genuinely still, which is what the Goal claims. (2) the new page's `sim: 17` budget (not the shared strict 8) came with a formula calling the rate "ordinary JIT/warm-up noise"; forcing the budget to 1 and reading `windowByFn` names it — `runOneTick` 1360 B, `warm` 1152 B, `resync` 900 B over 600 frames. **Per-tick allocation with names, not noise.** The number stands, the reasoning was rewritten, and `runOneTick`'s ~2.27 B/frame on a connected host tick is a new ledger row. **Both corrections are the same failure: a confident mechanism written into a repo comment, backed by something that happened to work rather than by a measurement that isolated the variable.** **M15e fixed the CI-only red that M15b's push left on `main`, and it was not the defect it had been recorded as** (`docs/plan/15e-paced-tick-measurement.md`, one implementer, no fix rounds): `SimHostCounters.ticksRun` is **cumulative from `simHost.start()`** and `poll_skips_a_spurious_tick_on_a_ring_wake` read it **once, at the end**, comparing a lifetime total against a *poke-window* expectation of 30. Inserting idle before the poke adds exactly one tick per 50 ms (26 / 48 / 86 at 0 / 1000 / 3000 ms, measured by the orchestrator on `efde638` before delegating), so the runner's ~2.5 s of page-load and IPC latency is the whole excess behind CI's 79 and 83. **The Blockers note it replaced had the reasoning inverted** — it argued a slow runner would tick *fewer* times, when a slow runner spends longer before the window opens and therefore ticks *more*; ADR 0030, `worker/sim.ts` and `atomics-timer.ts` were never implicated. The test now asserts the delta between two `__simCounters` readings, with `expectedTicks`/`0.5`/`1.35` byte-identical in the diff; `__simCounters` gained a `resumeWorkers` so pacing survives the before-reading, whose re-entry into `runBlockingLoop` contributes a known, commented **+1 tick**. **Failability was proved per-unit and re-run by the orchestrator**: guard reverted to an unconditional `poll()` gives 48 (implementer) / **47 (my own re-run)**, failing `< 40.5`; restored, 26-27 across 20 repeats. **The general lesson, and it applies to every counter on `SimHostCounters`:** a counter read once at the end of a window measures the lifetime, not the window, and the error scales with how slow the machine is *before* the window opens — which is exactly why it was invisible locally and reproducible only on `ubuntu-latest`. **Recorded, not fixed** (ledger): `server.ts`'s `resync()` corrects only when the sim is behind (`if (overshoot > 0)`) and has no branch for running ahead, so ADR 0030's "bounded at one window and self-correcting" holds in one direction only; candidate owner M36b. **M16 (action round trip) landed, cut 1-2 / 3-5 / 6-7, plus a review agent and gate rounds** (`docs/plan/16-action-round-trip.md`; its Deviations are now ~700 lines and hold every seam shape, measured number and finding -- read them, not this line, for detail). Cut 1-2 (Rust admit pipeline, `on_action`, outbox, result records, `ABI_VERSION` 10 -> 11), cut 3-5 (TS `dispatch`/action ring/clock block/`onActionResult`, `client_clock_stats` + ABI 11 -> 12, ts-rs bindings, `puts_script_a` made `.wasm`-authoritative), cut 6-7 (`slice.html` + HUD, `vertical_slice`, zero-GC page `zero_gc_action`, the `add-action-type` skill). **Five findings from this milestone are worth carrying.** (1) **A review agent found a real dedup defect two green gates missed:** `Host::on_uplink` snapshotted `last_seq` from the store, but the store only advances it inside `Store::apply` at the *next* tick, so two uplinks carrying the same `seq` between two ticks both admitted and both applied (a resent `Bump{n:5}` totalled 10, not 5). Fixed with `ConnSlot::highest_admitted_seq` folded against `Store::last_seq`. **The test that should have caught it, `resent_seq_is_dropped`, is the eighth instance of this repo's signature defect:** it resent only *after* a tick had applied the first copy, while its own comment claimed the general reconnect guarantee. Now split into `_after_apply` / `_before_apply`, and the new one deliberately uses an **additive `Bump` rather than an idempotent `SetTotal`, because idempotent state would mask a double-apply exactly as the old green runs did**. (2) **A deadlock and the fix that was worse than the bug:** three test pages awaited `client.ready` before installing the hooks that drive ticks, so once `ready` waited for `session_state = 1` nothing could drive them. The first fix spun `stepSimTickSync` until it stopped throwing, using "workers array non-empty" as a readiness proxy -- but that array fills *before* a worker finishes async setup, so the first main-thread-blocking call **starved the very worker boot it was waiting for**: 11,346 ms, ending exactly when the three `engine_init ok` fired. Waiting on a real `workersReady` promise took it to 82 ms. **Production was never affected** and the reasoning is worth keeping: `worker/sim.ts` only skips `simHost.start()` when `message.test` is present, and `options.test` is a test-only field no real app sets, so a production sim worker always paces itself and nothing on main needs to drive a tick. (3) **`pnpm test` was rewriting the committed ts-rs bindings and then failing `pnpm lint` on them** -- the gate defeating itself, plus a dirty tree after every run, because `export_bindings_*` tests are generated by the `TS` derive and run inside the ordinary `rust` suite. Fixed by pointing `.cargo/config.toml`'s `TS_RS_EXPORT_DIR` at a gitignored scratch dir; `buildGame`'s `exportBindings()` sets it explicitly and cargo's `[env]` does not override an already-set variable, so the real bindings step is untouched. (4) **A zero-GC isolate class chosen pre-emptively rather than measured:** `zero_gc_action`'s `main` shipped as `class: "budgeted"` (no *major* GC) with the rationale that JSON-parsing a result "allocates by construction" -- but assertion A counts **GC events, not bytes**, and `connected-terrain` reaches zero GC events at ~102 B/frame. Asked for the measurement, the honest answer was "picked to be safe, never measured `strict`"; `strict` then passed 8/8 with the `burst` control correctly tripping **A as well as B**. It is `strict` now. **On a `budgeted` isolate the burst control stops exercising assertion A, so half the instrument goes untested -- never take that class without measuring `strict` first.** The `gc/suite.ts` class-awareness fix found along the way is real and kept (the verdict table genuinely disagreed with `analyse.ts:228-229` for budgeted isolates and had never been exercised), now dormant. (5) **Edit `budgets.json` as text, never parse-and-reserialize:** a Python `json` round-trip with `ensure_ascii` silently re-encoded `§` as an escape across five untouched formula rows, which is exactly the churn that hides a changed number in a reviewed file. Restored byte-for-byte. **Measured and recorded:** uplink 12 B per one-action batch (3 B for the action itself); **0 B/action host-side allocation through the real admit path**, flat from 100 to 25,600 actions -- and the first attempt at that measurement read ~92 B/action purely because the harness never called `build_frame`, leaving `ConnSlot::pending_results` undrained, a test artefact rather than a leak. `zero_gc_action` budgets: `main` 115 hardware / 89 software, `sim` 18, `client`/`gen0` 8. **Accepted and not fixed, for the ledger:** a locally dropped action (malformed via `dispatchRaw`, or a full outbox) now increments a ring drop counter but produces **no `onActionResult`**, so that `seq` never resolves -- M25's pending queue owns real local rejection; and continuous real rendering plus `test.flags.pace` can starve `worker/sim.ts`'s `AtomicsTimer.poll()` for seconds under ADR 0030's wake guard (observed to recover, never permanently stuck; `worker/sim.ts` was outside M16's Files touched). **`puts_script_a` moved from a native-only `tests/golden/puts_script_a.hash` to the shared `golden/golden-script-a.json` that both the native test and the new `wasm_script_a_matches_native` read: the value `7bdddfc9c749b1fb` did not move, the file did, and it is now a continuous native-vs-wasm equality check like `puts_idle_100`.** **The last gate round was `vertical_slice`'s pixel flake, and the orchestrator's hypothesis was wrong.** `WALK` is 8 fixed tiles near the origin and can never reach the probed tiles. The real cause was **the page's own continuous render loop overwriting the probe's frame uniform between two separate `page.evaluate` calls**. The fix is an atomic `__probeTile` plus `__sliceSettle` (one rAF, then `untilQuiescent`, then `resumeWorkers`) in place of fixed waits (`67c67b4`). Failability was shown: splitting the probe back into two calls reproduced `got 34, want 30` under load. **The `add-action-type` skill was verified by a fresh Sonnet agent that never read the brief.** It found three gaps, all patched at the gate; the sharp one is that **a variant adding a field to replicated state moves every golden, including idle scenarios that never dispatch it**. **Final loops:** 15 quiet runs = 1 failure (`parkWorkers` timeout, again on `zero_gc_action neg object client`, which went **20/20 alone**, so this is suite saturation, not the page). Under `--load 10`, 9 of 15 runs failed **on the suite budget only**. That is the M16c problem.
- **Exact next step:** On a new machine run `pnpm install && pnpm setup:tools` first. Start on `main` with edits auto-accepted, confirm `milestone-implementer` is among the agent types. **Start M25** (loop step 3); read `gh run list --limit 3` first. **The fast `browser` suite is at 28-29 s of 48 s quiet and 36-39 s under `--load 10`**: any milestone adding browser tests keep each under 0020 §4's 3 s p95; the suite has headroom to about 45 s quiet (ADR 0036) . **Run `pnpm test:slow` yourself (hardware) at every tag milestone**; the next is M29 (`multiplayer-in-browser`). read `gh run list --limit 3` first. `pnpm test:slow` now ends with a `frame-bench` line; it gates only on real hardware. **Run `pnpm test:slow` yourself at every tag milestone from now on**, hardware mode, not only CI's software run. **At M16e's gate, if it ends at step 4 (not reproduced), that is an acceptable outcome**: the instrumented message is the product, and the next occurrence names the cause. **M16b touches the client clock and the UI ring, which is cross-thread, so cut it into step ranges of about three up front and name the seam fields when you cut.** **At M16d's gate:** check that the step-1 test really fails on base (re-run it yourself against the reverted fix), that `poll_skips_a_spurious_tick_on_a_ring_wake` still asserts the same thing, and run `repeat.mjs browser 15 --load 10` plus 15 quiet. **Do not ask an implementer to run `pnpm test` or `pnpm lint` -- you are the gate -- but *do* tell them targeted foreground runs are expected, including browser ones** (`pnpm test browser -t <pattern>`): a blanket "no `pnpm test`" in this session's first delegations is what let a cut hand back 11 browser failures it could have seen itself. **For CI, read the run rather than trusting a sha written here** — `gh run list --limit 3` — because any sha this block names as "current" is stale the moment the next commit lands, including a doc-only one. The last `done` commit's run was green; doc commits after it carry their own runs. **Cut M16 into step ranges of about three up front** — it is a cross-thread round trip (`dispatch` -> admit -> apply -> ack -> `onActionResult`) and that is exactly the shape the cut rule exists for — and **name the seam *fields* when you cut**, not just the files. **Pushing no longer prompts Tyler** — push every `done`-green head, and verify green *before* pushing (`5cbf35e`/`e6767d6` are red precisely because an orchestrator committed a record edit without re-running; run `pnpm test && pnpm lint` again after your own record edits, before every `done` commit). **When CI is red after a green local gate, the runner is usually right and the repo is usually wrong:** M10's Rust-pin finding was a real defect latent on any machine whose defaults differ — fix the defect, never the workflow. **M15b's push proved it again:** the local gate was green and 15 local repeats were clean, yet `ubuntu-latest` ran 83 ticks where 30 were expected. **But read M15e before reusing that example, because the follow-up is the real lesson:** the *runner* was right and the *test* was wrong — `ticksRun` is cumulative from `simHost.start()` and was being compared against a poke-window expectation, so a slower runner accumulated more, not fewer. The note that stood in this block for a day reasoned the opposite ("a slow runner would run fewer ticks") and would have sent an implementer hunting a spurious-tick defect that never existed. **A CI-only red is not automatically a defect in the thing the test names; measure what the test actually measures first.** **Read the CI run of every `done` push before you start the next milestone** — M15's was green, M15b's was not, and nothing local would have told you. **When the fix really is a time limit, say why it is not a mask.** **A milestone with CI rounds is run by keeping one implementer alive:** it commits and stops, you push and hand the run back with `SendMessage`, its context intact — M10 took ten rounds that way. **Cut a renderer or cross-thread brief into step ranges of about three up front** (M09b's seven steps were cut 1–3 / 4–5 / 6–7 and *still* took two fix rounds, while **M13 was cut 1–3 / 4–6 and took none**), and **when you cut, name the seam *fields*, not just the files** (M11's `InputEventTs.button`/modifiers and idle-mouse `hover` fell between two delegation prompts and shipped hardcoded to zero). A single-crate Rust milestone is not cut: **M14 was one implementer for all seven steps and that was right.** **Hunt for tests that cannot fail — it is this repo's recurring defect, seven times now:** M09b's probe grid sat at texel centres, the one point where a correct and an inverted sampling formula agree; M10 found every zero-GC negative control firing *outside* its attribution root, so software-mode assertion B had never once been exercised (ADR 0029); M13 declared counters that can only ever read 0; **M14's `encode_decode_no_alloc` reached only 4 of its module's 8 files**, so three of them carried no allocation guarantee while the brief's budget and the module's own `CLAUDE.md` claimed one for all of it. **M14's is the one the gate could not have caught, and it is the argument for the ≈3,000-line review rule:** every suite was green, and a green suite cannot report its own blind spots — the review agent found it by *injecting* an allocation into `write_global` and watching the test still print `ok`. **So when an implementer proves a test can fail, ask which file or branch the proof covers, not whether it passed:** M14's original proof was honest, correctly executed, and covered one function inside the covered half. The fix is per-unit proof — eight inject-fail-revert cycles, one per file — and **re-run one of them yourself** (I re-ran the `write_global` injection: 48955 vs 48827, fails as it must). **A sixth instance landed in M15d and it is the cheapest to spot:** the implementer backed a claim — written into a *production source comment* — with a diagnostic whose only assertion was `expect(true).toBe(true)`, so its green run proved nothing; re-running it and reading the numbers took one command. **When a report cites a test as evidence, look at what that test asserts.** **The seventh landed in M15c and it is the subtlest: a test that passes for a reason other than the one it claims.** `overlay_tile_reaches_screen` exists to show terrain reaching the screen **with the camera held still** — its milestone's own Goal — and it passed only because its helper nudged the camera a tile away and back. The helper's doc comment explained the nudge with a confident, false claim about a production bug. Isolating the variable took one coordinate change: with both calls at the *same* position it still passes, and with no extra frames it fails, so the mechanism was the frame count against an async gen round trip, never the camera. **Ask what a passing test would still pass without, and whether that is the thing the milestone claims.** **And a fix that is only proven *inert* is not proven:** M15b's steps 1-3 landed ADR 0030's wake discrimination while nothing could yet wake the worker externally, so I required a test that makes it live — that test is what CI then failed on. Require it every time. **Three techniques from this session, in the order they paid:** (1) **`--no-opt --no-sparkplug` added to the `gc` project's `--js-flags` in `playwright.config.ts` turns an intermittent, CI-only zero-GC red into a deterministic ~15 s local command** — M15f reproduced CI's 108.5 and 28 as 108.52 and 28 on the first try, after the same number had sat unexplained since M06b. Revert the edit; it is a diagnostic, never a commit. (2) **Force a budget to 1 (or -1) to make a passing zero-GC test dump its `windowByFn`** — that is how M15c's `sim: 17` was attributed to `runOneTick`/`warm`/`resync` rather than left as the "JIT noise" its formula claimed, and how M15f proved `stepSimTickSync` was *gone* from `byFn` rather than merely smaller. (3) **When a fix is verified in a mode the local gate does not run, re-inject the original defect as a control first** — a green `GC_MODE=software` run proves nothing if the env never reached the child process; M15f's software verification was only trustworthy because re-injecting `"software": null` reproduced CI's exact error. **And check the inert-looking fields in a budgets diff, not only the interesting numbers:** M15c shipped `"software": null`, the only null in the file, past a gate where I re-derived the `sim` formula byte by byte — `gc/instrument.ts` *throws* on a null software block rather than skipping the page, and CI runs `GC_MODE=software`. One scan across pages would have caught it. **Do not tell an implementer to "take ADR 0020 §4's next rung" without checking one exists:** at M15c's gate, ranking all 110 browser tests showed 58.6 s of summed work with a longest test of 1.79 s against §4's own p95 ≤ 3 s criterion, so nothing qualifies for demotion and the ladder is exhausted; an implementer told to find a rung can only comply by damaging coverage. **Measure an explanation rather than accepting it** (M13's `gc-sim` `main: 0` arrived explained as inlining, ADR 0029's exact failure mode; one command settled that the number was right and the reasoning was wrong — **and the reasoning is what a later session would have acted on**). **Make the implementer name which counters are live** (M13's `genOnMiss`, `chunksWarmed`). **`pnpm gate` refusing a changed golden is the system working, not a veto** — check the *value* did not move and that coverage got **stronger**, then approve it deliberately. **Take a cheap-window fix while the window is open** (M13's `tick_hz`), and require a test that exercises the new path. **Split rather than tick a headline outcome that did not happen:** M15b's own Goal was overlay tiles reaching the screen, blocked by a pre-existing M07/M08b bug, so it was ticked for what it landed with its criteria amended *in writing* and the rest moved to M15c, whose brief I wrote — and M16's `After` moved with it, because the vertical-slice marker claims that outcome. **When you rule on a finding, rule on the remedy too:** Opus recommended bounding the undrained cache-event queue, which would have given clients missing terrain with no signal; opt-in recording was the safe shape. Ask what a proposed fix does when it is wrong. **The no-allocation assertion template is M15's, not M04's:** assert growth at a long window **equals** growth at a short one (no budget number in it to widen), and express real growth **per unit of new work**, never per tick — a per-tick ceiling passes silently when cost per unit doubles as the rate halves. **Welcome findings in already-accepted code** (M13 found `simSealFrame` allocating every tick in M06b-era code). **Look for masks in every fix round before reading anything else** — M14's fix round raised its corpus bar from 500 against a measured 2624 to `>= 2600` and added a new corpus, which is the shape a good fix round has; under a red gate implementers have instead reached for a longer timeout, a widened budget with "headroom", a halved tick rate and a production busy-wait. **Expect implementers to end turns with background work running** despite being told not to (four have now): check `pgrep` before touching the tree. **And when a report offers you options, notice how many of them are ways of not fixing the thing** — M11's steps 6–8 offered "accept the flakiness", "investigate", or "demote the test to `@slow`"; two of the three were exits, and the one real option took four rounds and found a V8 JIT artefact underneath. When a milestone adds cross-thread tests: repeat `browser` about 30 times plain and 30 under `--load 10` (Rules; M13 measured 30/30 and 30/30 clean). **This is not a formality:** M09b's implementers called a `viewport: resize renders same frame` failure an environment hiccup and the loops measured it at 2 failures in 16 quiet runs — it failed *quiet* and survived `--load 10`, so run both and read the asymmetry. **M14 needed no repeats: it is pure Rust and adds no cross-thread or timing-sensitive test** — spend that time only where concurrency is actually new. New zero-GC pages get `@slow` bursts automatically. Run the machine-hygiene commands (Rules) before any gate. **Get a Sonnet review agent on any renderer or cross-thread diff over about 2,500 lines, and on any diff over about 3,000** (M14's was 3,297), briefed to hunt for tests that cannot fail rather than only for defects. **Verify a Tyler-facing command literally yourself** rather than accepting an implementer's substitute. **Do not ask an implementer to run `pnpm test` or `pnpm lint` at all — you are the gate.** Five have now ended turns waiting on background suite runs they started against instruction; M15's burned three turns and a large part of one implementer's context before it handed anything back. Tell them foreground targeted runs only (`cargo nextest`, `pnpm test <suite> -t <pattern>`), nothing in the background, and that a red suite is not their problem to clear. **Measure the explanation, not just the number — M15 is the sharpest case yet.** A new zero-allocation test measured a flat ~95 B/tick; two rounds explained it as `BTreeMap` alloc/free churn and both were confidently wrong, because the instrument counts **live** bytes (allocated minus freed) in which a matched alloc/free pair is exactly zero. **One grep at `src/abi/arena.rs` settled it.** When a report explains a number with a mechanism, check that the mechanism can produce that number *in that instrument* before spending another round on it. **Two rounds then Opus is a real rule and it paid here:** the Opus round attributed the whole rate with remainder exactly 0, split host from client (the client allocates nothing), and found a third grower — an undrained cache-event queue — that neither Sonnet round nor I had anticipated. **When you rule on a finding, rule on the remedy too:** Opus recommended bounding the queue, which would have given clients missing terrain with no signal; opt-in recording was the safe shape. Ask what a proposed fix does when it is wrong. **The no-alloc assertion template is now M15's, not M04's:** assert growth at a long window **equals** growth at a short one (per-tick growth exactly zero, no budget number in it to widen), and express any genuine growth **per unit of new work** — per newly reached chunk — never per tick, because a per-tick ceiling passes silently when cost per unit doubles as the rate halves. **A gate red is not automatically the milestone's:** M15's two `gc-sim-paced` failures reproduced at the same 1-in-6 rate on the *base* commit under `--load 10`, and M15 had changed zero TS/JS. Bisecting against the base took two commands and stopped a pre-existing flake from being charged to a clean milestone. **Pacing, for calibration:** the previous session landed M10, M12, M12b and M13 in about 34 % of a 1M context, and M10's ten CI rounds cost roughly as much as the other three together; M14 including its review agent and fix round cost about 3 %; **M15, with one implementer, three fix rounds, a review agent and a doc sweep, cost about 13 % — the three rounds chasing one allocation number are most of that**. **Pacing across the last two sessions:** the one before last landed M15, M15b and M15d (three milestones, five fix rounds). **The session that just ended landed M15e, M15c, M15f and M15g — four milestones, three fix rounds — for about 34 % of a 1M context** (measured with `/context` at the point M15g was specced; loop step 7's stop threshold is roughly 50 %, so that is a little over half the budget for four milestones). **All three fix rounds were M15c's** — M15e, M15f and M15g each needed none, because each was diagnosed to a single named function or field *before* being delegated rather than after a round of guessing. **A brief you write yourself at a gate is cheap and pays for itself: M15c, M15d, M15e, M15f and M15g were all written that way and all five were delegated with no further clarification needed.** The next marker is M20b (`first-playable`).
- **Blockers:** **none open.** **Machine watch item (new at M23's gate, not a repo defect):** since 2026-09-25 19:55 Tyler's Mac has had five `Google Chrome for Testing` crashes (`~/Library/Logs/DiagnosticReports/Google Chrome for Testing-2026-09-25-*.ips`), all identical: `EXC_GUARD` `INVALID_OPTIONS on mach port 0` on `CrBrowserMain` inside AppKit's accessibility notification path (`NSWindow` title change → `NSAccessibilityPostNotification`), no engine frame on the stack. It shows up as `Target page, context or browser has been closed` (or a `page` fixture setup timeout) on an unrelated test after a normal predecessor: 1 in 30 quiet and 2 in 30 under load at M23's gate, 3 in 30 quiet and 0 in 30 under load at M24's, every failure matched to a crash report. **Second watch item (new at M24b's gate):** `reference_ui_smoke_collect_and_inventory` (M20b) timed out on `button.click()` once in 15 runs under `--load 10`, in a run whose suite took 65 s, with no crash report at that minute; the reference test page does not enable persistence, so no M24b code was on its path. First occurrence; if it recurs, attribute it. To classify a loop failure, keep its report (a scratch copy of `scripts/repeat.mjs` that `cpSync`s `test-results/browser` on failure) and check `~/Library/Logs/DiagnosticReports` for a report at that minute. CI (Linux) cannot hit it. A crash at 07:53 on 2026-09-26 with the display on ruled out display sleep; every stack is Chromium tearing down a window view while posting accessibility notifications (`NSView _setWindow:` → `NSAccessibilityRemoveAllObserversAndSendDestroyedNotification`), on macOS 27.0 (26A428). Asked Tyler 2026-09-26 which apps hold Accessibility permission and whether one started 2026-09-25 afternoon; do not chase it in engine code. **New at M18's and M19's gates (quiet loops):** one `stepping` hash mismatch (M18c: not reproduced in 195 runs, diagnostic committed, ledger watch) and `park('sim'): timed out` on `gc: flat transport parity` with the worker `Armed` (**fixed by M19b**: a lost park notify; quiet loops were 30/30 after the fix). A `parkWorkers` timeout with a *different* signature is still the watch item below. If one comes with evidence that the worker crashed, read ledger row "crashes after setup" first. M15f closed the last one, and it had been open since M06b. **Watch item:** `parkWorkers: timed out after 10000 ms`. It now appears in **both** tiers and **without injected load** — 1 in 30 fast-tier runs at 24 s (where the pre-M15c base measured 15/15 clean at 20 s), and once in the slow tier on `terrain: probe tile colours webkit @webkit-gpu @slow` at a 1-minute load of 6.87, clean on the immediate re-run. It is a harness timeout, not an assertion, and `@webkit-gpu` does not run on Linux, so the slow-tier instance cannot reach CI. **At M17's gate it named `client` as the stuck worker for the first time** (`W_PARKED` 0, `W_WAKE` 1776 against `W_ACK` 1500, `sim`/`gen0` parked): a client kept busy by a steady wake stream while park is requested, unlike M16e's caught-up `gen0`. **Do not lengthen it** — it is the only thing measuring this saturation, and the exhausted suite-time ladder above is the real signal underneath it. **Never widen a zero-GC budget to clear anything** — ADR 0029 makes a budget that stops a control tripping the exact failure mode to avoid; M15d and M15f are the worked examples of the alternative, fixing the allocation and leaving the budget alone or re-deriving it *downward*.

**Keep the State line from growing without bound.** It is a running summary, not a log: when a milestone is two or three milestones old and its brief's Deviations hold the detail, compress its entry to the names a future session needs to grep for. M04–M08b were compressed this way at M11's `done`.

Overwrite this block (never append to it) and commit it twice per milestone: before delegating (`M<NN> start`; Milestone reads "M<NN> in flight, base `<sha>`", the sha of the commit before it) and after accepting (`M<NN> done: …`; Milestone names the next row). Leave the rest of this file intact.

## Your job

You are the **orchestrator**; run this session on Fable or Opus. Land as many milestones as fit, in `PLAN.md` order, one after another. You pick, brief, check and record. You do not write the milestone's code and you do not read its reading list: a Sonnet sub-agent does (ADR 0025).

## The loop

Once per session:

1. **Read** `docs/process.md` ("Rules for every session"), `PLAN.md` ("How milestones work" and the table) and `.claude/agents/milestone-implementer.md` (what your implementer already knows, so your prompts can stay short). Nothing else up front.
2. **Check the ground.** You are on `main`, and `pnpm test && pnpm lint` is green. If Status says a milestone is *in flight*: `git log <base>..HEAD` step subjects plus the brief's Deviations say how far it got. A dirty tree is handed to a fresh implementer as it is (never stash or reset); continue at step 4 with "continue from step k". A dirty tree with nothing in flight is not yours to clean up: ask Tyler.

Per milestone:

3. **Pick** the first unticked `PLAN.md` row whose **After** rows are ticked. A row that needs Tyler first (a push, a deploy, a login, an answer) is skipped while another row is ready. Read its brief, and only the brief. Overwrite Status and commit `M<NN> start`.
4. **Delegate** to `milestone-implementer`. The prompt is the brief path, a step range if you are cutting, and what it must know from earlier Deviations that the brief does not say. If that agent type is not registered, use `general-purpose` with `model: sonnet` and tell it to read `.claude/agents/milestone-implementer.md` first and follow it. Foreground, one at a time: there is one working tree, one cargo target and one set of test ports, so no other agent builds or tests meanwhile. Every other delegation (research, review, ADR draft, doc sweep) is `general-purpose` with `model: sonnet`, briefed with files (ADR 0021 §3). Two exceptions are written into their briefs: M39 and M39b are run by you, because they are delegation and judgement and a sub-agent cannot launch sub-agents; in M10 you push for the implementer, which never pushes.
5. **Accept.** The report is a claim; check the repo:
   - `pnpm gate <base>`: tree clean, files changed, existing goldens modified or deleted, skip/ignore/only/todo markers added, diff size.
   - `pnpm test && pnpm lint`, run by you. For slow-tier commands accept the implementer's pasted result line; re-run them yourself only at a tag milestone.
   - The changed files sit inside the brief's "Files touched"; every name under "Tests added" exists (`grep`); every **Provides** name exists (`grep -n`; do not read the files). A diff over roughly 3,000 lines gets a Sonnet review agent, not your read.
   - A criterion that says "by hand" or "in this session" needs automated evidence (`playwright-cli`, a Node `fetch`, a pasted command line with its output) or it stays unticked and goes under "Criteria awaiting Tyler" in `docs/plan/questions-for-tyler.md`. Never tick on a claim. Like a device check, a criterion that only Tyler can verify does not block: when it is the only kind left unticked, the milestone is still recorded as done.
   - **Decisions needed** in the report: a technical one is yours (decide, record it per the Deviations rule below, send the implementer back with the answer); one that is Tyler's goes into `questions-for-tyler.md` with a recommended default, and the implementer continues on the default.

   Failures go back to the same implementer (its context is intact) or, if it is unavailable or near its limit, to a fresh one after you write the failures under "Open gate failures" in the brief's Deviations. Two rounds at most; then one implementer with `model: opus`; then split the milestone or record a blocker. Your own fixes: under about 20 lines, in one file.
6. **Record.** Tick the brief's exit-criteria boxes and set its `Status:` to done; tick `PLAN.md`. A changed decision gets an ADR drafted by a Sonnet agent with the `write-adr` skill and reviewed by you. Deviations that touch later briefs: a Sonnet agent fixes those briefs from the Deviations text and you review `git diff --stat`. `questions-for-tyler.md`, coverage rows and `deferred-ledger.md` are yours (or a Sonnet doc agent's). Overwrite Status; commit `M<NN> done: …`. On a marker row (`PLAN.md` names the tag): `git tag -a <tag> -m "<what is true now>"`.
7. **Continue** at step 3. **Stop** at a milestone boundary when context use passes roughly 50 % (if you cannot see it: after six milestones, a milestone that needed a fix round counting double), or when every ready row needs Tyler. On stopping tell Tyler: milestones landed, tags made, device checks ready (`docs/plan/device-checks.md`; they never block the next milestone), criteria awaiting him, open questions, and that `main` is ready to push.

## Rules

- **Oversee, don't build.** If you are about to read a reading-list file or write milestone code, delegate instead.
- **Goldens, tests and checkboxes are evidence.** A changed existing golden or a weakened test is your decision, never the implementer's. You are the only writer of exit-criteria boxes, `PLAN.md` and this file.
- **Trunk only.** Everything is committed on `main`; no branches. `main` may be red between step commits and is green at every `M<NN> done:` commit (last green: `git log --grep ' done:' -1`). **Push only `done` commits** (M10's CI runs on every push); pushing is not allowlisted, so it prompts Tyler.
- **Deviations are written down, not drifted into.** If the brief is wrong, too big, or contradicted by what the implementer finds: a small correction goes in the brief's **Deviations** section (and later briefs it affects are fixed); a changed decision gets a new ADR that supersedes or amends the old one (never rewrite an accepted ADR) plus a line under "Plan-level decisions" in `PLAN.md`; a milestone that does not fit one implementer is split (`<NN>b-<slug>.md`, new row in `PLAN.md`, written by you) rather than overrun.
- **Seams are contracts.** Names under a brief's **Provides** are consumed by later briefs. If one must be renamed, grep `docs/plan/` and update every consumer in the same commit.
- **Context budget.** Yours is spent per milestone on one brief, one report and a few command lines. Stop at a milestone boundary (loop step 7), never in the middle of a gate.
- **Questions for Tyler** are batched in `docs/plan/questions-for-tyler.md` with a recommended default; proceed on the default and mark what depends on it. Scope, taste, cost, and anything that changes a Requirements section in `docs/spec/` are Tyler's; technical choices are yours, recorded in an ADR or the brief.
- **Budgets** live in `PRE-PLAN.md` §7 (by ADR) and, once M04 lands, in `packages/engine/budgets.json` (what tests assert). A milestone that cannot meet a budget says so in Deviations and opens the question; it does not quietly raise the number.
- **A report is a claim, and so is "flaky under load".** M06's livelock and M06b's per-pass boxing and lost wake were each first explained away as scheduling noise. When a milestone adds cross-thread or timing-sensitive tests, repeat its suite yourself before accepting: `node scripts/repeat.mjs <suite> <runs> [--load <n>]` (per-run kill timeout, self-terminating burners; a Bash call caps at 10 minutes, so `browser` in batches of 15–20, in the background), about 30 runs as the machine is and 30 under `--load 10`, 0 failures and 0 hangs. A hang that outlives the runner's timeout with one thread at 100 % CPU is a synchronous spin, not contention.
- **Machine hygiene before any gate and after any report (or missing report).** Suite budgets are wall-clock and the machine is shared: `uptime` (at a 1-minute load near 20 every suite fails on timeouts only), `pgrep -x yes | wc -l` (orphaned load generators: must be 0), `lsof -ti tcp:4517` (an orphaned `vite preview`, which Playwright would reuse: kill it), `pgrep -fl "vitest|scripts/test.mjs|scripts/gc.mjs"`. A `playwright-cli` daemon may belong to another session: leave it. Say "loops run in the foreground, bounded, with a per-run kill timeout; no background load generators" in every delegation that involves loops: implementers wait on background tasks, get lost and leave orphans.
- **Look for masks in every fix round, before reading anything else:** diff `budgets.json` numbers, grep the diff for timeouts, retries, new constants and reduced workloads. Under a red gate implementers have reached for a longer timeout, a widened budget with "headroom", a halved tick rate and a production busy-wait.
- **Quantify before hypothesising.** `bytes ÷ wakes ≈ 14.7 B` named one HeapNumber per pass, and a fixed 136 B delta named a one-off, before anyone opened the code. Ask for per-function attribution first and make the implementer follow it; state your own mechanism guess as a guess.
- **The commit gate lints the whole tree,** so dirt left by a lost implementer blocks your own commits. Hand it to the next implementer uncommitted (loop step 2); never reset it.
- **Verify, don't recall.** Browser, tooling and Claude Code behaviour change quickly; have current docs checked before relying on an API or flag.

## When Phase 3 ends

`docs/plan/39-acceptance.md` checks the Phase 3 exit in `docs/process.md` (the reference game works in single-player and multiplayer; the full test suite passes within budget); the last milestone, `docs/plan/39b-phase-4-handoff.md`, rewrites this file for Phase 4 and ends with the `phase-3-complete` tag.
