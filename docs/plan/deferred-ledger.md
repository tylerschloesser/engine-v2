# Deferred-items ledger

Every row of `PRE-PLAN.md` §10 (same order), with where Phase 2 settled it. **Decided** = the rationale is in the named brief's *Planning decisions* section or the named ADR. **Owned** = needs code or a device; the named milestone carries the question as an exit criterion. Nothing is unowned.

| Item (`PRE-PLAN.md` §10) | Outcome | Where |
|---|---|---|
| Typed fast path + quantized deltas for continuous action streams | Decided: not built, not scheduled (no planned game has such a stream); uplink flag bits 2-7 kept free for a future stream record; revisit when a game sends an action every tick, which gets its own ADR | M14 |
| Zero-GC: a negative control on one isolate raises a *sibling* isolate's reading (M06b, open) | **Live and now the leading explanation for the last intermittent CI red.** On `gc-sim` under a `sim` burst, `main` attributes 28 B/frame against a software budget of 0 (raw 49.99), unchanged by M13b and within noise of the pre-M13b reading, so it is not the `stepTick` allocation that explained the `sim` side. Was our working theory for two rounds at M14's gate and was *wrongly* applied then — the numbers there were fully explained by `stepTick`. Here they are not. Next step is attribution from a red run's `windowByFn` (in every failure message since `2024709`; untruncated in the uploaded artefact), not another local reproduction attempt | open; owner unassigned |
| Presence as an optional replay track | Decided: not built | M19 |
| Determinism on real x86-64, physical iPhone, Android | **x86-64: closed.** Native (`scenario_matches_golden`), Node, Bun and Chromium all matched the Apple-silicon goldens on `ubuntu-latest` (run [35616810284](https://github.com/tylerschloesser/engine-v2/actions/runs/35616810284)); Linux WebKit and Firefox (`@engines`, `pnpm test:slow`) also matched, closing ADR 0002's own deferred item in full (run [35618167031](https://github.com/tylerschloesser/engine-v2/actions/runs/35618167031)) -- since ADR 0026, this CI leg is the only thing that proves three-browser determinism at all, `pnpm test` alone no longer does. The iPhone via the determinism page; Android not run: no device (Q5) | M10; M03 device check |
| `+simd128` and `wasm-opt` for the sim module | Owned: both stay off until measured against the goldens | M36b |
| Provisional ids for predicted entities | Decided | ADR 0022 (implemented in M25) |
| Taint rule after a `NotPredictable` pending action | Owned: three candidate rules run against fixed scenarios; the selection rule is fixed in the brief | M25 |
| Per-action growth declaration | Decided | ADR 0023 (implemented in M21) |
| Per-frame overlay change list + `predicted` flag | Decided: tile-only `OverlayDiff` into the dirty-chunk set; `FrameView::is_predicted` / `predicted_tiles` | M26 |
| Host-side atomicity of `apply` via an undo journal | Owned: built and benchmarked; adopt at ≤ 10 % `apply` overhead and zero steady-state allocation; ADR either way | M21b |
| Own-timer completion gap of one RTT | Decided: stretch the bar over `duration + lead` (Tyler, Q10); the remaining gap is measured | M26; feel check at M34 |
| Lead estimation; iterating reads; `entity(id)` gone vs unsubscribed | Decided: median of the last 8 per-ack samples; `WorldRead::entities_in` in ascending id order; ADR 0022 §7 | M26; M21 + M25; ADR 0022 |
| Exact `TickCx`, `FrameCx`, `FrameView`, `OldStore` shapes | Decided | `TickCx`: M12b (completed in M21b); `FrameView`: M17; `FrameCx`: M18; `OldStore`: M24b |
| Final check of reference-game coverage against the engine feature list | Decided: table written; uncovered features get fixture tests; one addition made, "pick up an empty furnace" (R3, added to the Requirements by Tyler; M33b) | `docs/plan/reference-coverage.md` |
| OPFS append/flush latency on iOS Safari | Owned: thresholds that retune the sync interval are fixed in the brief | M23 device check |
| Helper that rescales `Tick`/`Ticks` fields during `migrate` | Decided: `Rescale` + hand-implemented `RescaleTicks`; engine timers rescale automatically | M24b |
| Entity store layout and `EntityId` reuse policy | Decided | ADR 0022 (implemented in M12) |
| Overlay promotion to dense at 512 entries; bucketed per-chunk area effects | Decided: neither is built in Phase 3; promotion is a representation-only change with M36's memory high-water mark as its trigger; area effects need an ADR from the game that wants them | M07 |
| On-device validation of the 64 MiB world-budget split | Owned: `memory_bytes()` in M07, init-time sum in M21, high-water assertion in M36, device run in M39 | M07 → M39 |
| ms per chunk on a real iPhone and mid-range Android | Owned: `worldgen-bench.html` on the iPhone (Android not run: no device, Q5); thresholds that rescale the warn level or reopen the gen-worker count are in the brief | M08 device check |
| Sampled pristine-hash check between client and server | Decided: none; a dev-build assertion compares gen-worker output with local generation; the `Hashes` section stays extensible | M08, M31b |
| Whether noise helpers move into the engine crate | Decided: yes, `engine::noise`, f64 only | M08 |
| Durable Object adapter and feasibility check | Owned: recipe package + go/no-go rule fixed in advance; ADR from the result | M38 |
| WebTransport adapter | Decided: not built; revisit when Node LTS or workerd ships a stable server and the iOS floor has it, or when stalls exceed the interpolation cap | M27 |
| Real frame sizes against the bandwidth budget | Owned: counters in M15, asserted against `budgets.json` in M31, reference-game measurement in M36b | M15 → M31 → M36b |
| Engine-side byte diffing of old vs new values | Owned: measured on the busy furnace field; build only at ≥ 40 % saving (would be a new milestone `36c`) | M36b |
| Exact section ids, varint coordinate coding, overlay run format | **Closed by M14.** Fixed by the encoder and its golden bytes (`golden_section_ids`, `golden_coord_list_negative_and_far`, `golden_overlay_runs_literal_and_repeat`); table lives in `crates/engine/src/wire/CLAUDE.md` | M14 |
| "Copy my player link" identity escape hatch | Decided: not built; the invite-fragment parser ignores unknown parameters so it can be added | M28 |
| How iOS Safari reports a worker-owned socket after resume | Owned: on-page link log; tunes only 0013's dead timeout and probe deadline | M29 device check |
| Final export list per role, region ids and sizes, status codes | Decided: initial list in M02; single owner is `crates/engine/src/abi/registry.rs` mirrored by `src/abi.ts`, with an `ABI_VERSION` bump and the `abi-registry` test; each milestone adds its own exports there | M02 |
| Where `engine.log` text is decoded | Decided: in the instance's own isolate, by the loader (`LoaderHooks.onLog`) | M02 |
| On-device memory ceilings | Owned: memory page with arena-size URL parameters as the fallback | M11 device check |
| Ring capacities, uplink poll period, control-block layout, `yield` protocol | Decided | M06 (capacities, control block), M06b (`yield`), M29 implements the 10 ms uplink poll |
| Verifying COOP/COEP listings on one real static host | **Unverified, carried forward.** The Cloudflare Pages deploy was not approved (Q6); M38 serves the client from the Fly machine (`games/reference-server --static` sets the headers), which does not verify a static host's listing or the cross-origin `wss` sentence of 0015 §3 | M38 (states it); M39b (open item into Phase 4) |
| Whether the periodic snapshot `write` is inside the strict zero-GC window | Owned: inside if the sim worker meets its 0016 §1 budget with one forced snapshot in the window; otherwise a budgeted event with a superseding ADR | M23 |
| Final main-thread B/frame number and overlay-anchoring string constant | Owned | M17 (`gc.pages.drawables`), M18 (`gc.pages.anchors`) |
| Software-adapter form of assertion B | Owned: mechanism (`GC_MODE=software`) in M04, numbers in M10 | M04, M10 |
| Packaging-spike untested items (`server.fs.allow`, recursive `fs.watch`, posted `Module` in real Safari, `ts-rs` zero bytes) | Owned; **recursive `fs.watch` on Linux closed**: `plugin-dev: touch triggers rebuild and full-reload` passed on `ubuntu-latest` (run [35618167031](https://github.com/tylerschloesser/engine-v2/actions/runs/35618167031)) -- recursive `fs.watch` works on Linux, M02b's `watchCrate` needs no per-directory fallback, and the anticipated risk retires | M02b (config-level + touch test), M10 (Linux watch, closed), M11 device check (Safari), M35 (`link:` tarball cell, `ts_rs` symbol check). Windows `fs.watch`: unclaimed by design, no Windows target in Phase 3 |
| Whether `crates/` holds one crate or several | Decided: one crate `engine`; three named triggers for a split, any split needs an ADR | M01 |
| Real release build time and size; intermediate profile; `debug = "line-tables-only"`; snapshot → reload → restore on Rust edit | Owned / decided: build numbers and profile choices in M35 (ADR "Build profiles, measured"); restore-on-edit is not built, one slow test shows the 0005 upgrade path covers it | M35, M37 |
| Zero-GC harness shape run by hand in Safari and Firefox | Owned | M17b device check (desktop) |
| Terrain shader fill-rate on real phones | Owned: fallback order fixed in the brief | M09b device check |
| Exact WGSL, manifest JSON schema, upload-ring record layout, worker frame clock | Decided | M09 (bind groups, `tiles.json` v1, upload record), M17b (`sprites.json` v1), M06b (frame clock) |
| Overlay anchoring on iOS Safari | Owned: fallback `anchorMode=translate` | M18 device check |
| "Follow with user offset" | Decided: not in v1 | M11 |
| Input-ring record layout, easing curves, wheel constants, `FrameCx` shape | Decided | M11, M18 |
| Spike B: SwiftShader WebGPU on `ubuntu-latest` | **Closed.** `channel: 'chromium'` (new headless) returned a null adapter (run [35609763337](https://github.com/tylerschloesser/engine-v2/actions/runs/35609763337)); fallback rung 1 (`channel: 'chromium-headless-shell'` under `ENGINE_GPU=swiftshader`) gave the real answer: `{"vendor":"google","architecture":"swiftshader","device":"","description":"","isFallbackAdapter":true}`, with every M09 readback scene, pixel probe and the whole `gc` project (including the `terrain`/`input` zero-GC pages) green against the Apple-silicon goldens, no per-adapter-class golden needed (run [35616810284](https://github.com/tylerschloesser/engine-v2/actions/runs/35616810284)). Rungs 2-4 never reached | M10 |
| Spike C: byte-identical traces over loopback `ws` | Owned | M29 |
| Measuring the 30 s rebuild and per-suite numbers; sccache vs shared `CARGO_TARGET_DIR` | Owned: numbers recorded by each harness milestone, final table and decision in M36b; neither cache tool until one of M02's triggers fires | M02 → M36b |

## Gaps listed under the table in `PRE-PLAN.md` §10

| Gap | Outcome | Where |
|---|---|---|
| `createClient` option selecting single-player vs a server URL | Decided: `host: { kind: 'local', world } \| { kind: 'remote', url, joinKey? }` | M06b (M29 adds `remote`) |
| How `client.input` events and the input ring surface inside `FrameCx` | Decided: the slice `cx.input()`; TS → `ClientSide` via `client.input.emit` | M18, ADR 0024 §7 |
| How the main thread learns `Welcome.last_processed_action_seq` | Decided: `seq_seed` in the `SabSet.clockBlock` layout | M16 (M28 switches the source to `Welcome`) |
| Whether `dispatch` before `Welcome` queues or fails | Decided: throws; `client.ready` waits for a live session | M16 |
| The `TileTexel::from_tables` registration call | Decided: `Registry::set_base_visual` / `set_resource_visual` | M09 |
| Where test files live | Decided: per package; goldens with their fixture | M01 |

## Added during Phase 3

| Item | Outcome | Where |
|---|---|---|
| `browser` suite headroom: 17 s of its 25 s budget at M06b, and M08b, M09, M13 and M16 each add a zero-GC page (a clean test plus two negative controls per isolate) | Owned: not a gate failure at M06b. At M08b (63 tests): 19 s on a quiet machine, 25 s (the budget) under `--load 10`, so the next milestone that adds browser tests trips it. Trip-wire: the first milestone whose quiet-machine `browser` line passes 20 s demotes per 0020 §4 (multi-engine repeats first) before it is accepted; whether production-topology `burst` negatives may move to `@slow` while `object` stays in the fast tier is a 0016-vs-0020 question settled then, by ADR. Otherwise the suite audit decides. **Tripped at M09** (24 s, 80 tests): WebKit/Firefox repeats moved to the slow-tier `engines` leg and generated `burst` negatives to `@slow` for every page but `gc-loop` (ADR 0026, which settles the 0016-vs-0020 question); `browser` is 15 s of 25 s with 60 tests, 20 s under `--load 10`. The wire stays armed at 20 s quiet: the next rungs are 0020 §4's, in order. **Reached again at M13b** (98 tests): `gc-sim-paced`, the first page to exercise the production `onFire` pacing path, took the quiet line from 19 s to **20 s of 25 s** — the armed threshold, one milestone earlier than expected (M15b was the forecast). Not demoted: 20 s is the trigger to decide, not a failure, and M13b was a defect fix that could not be deferred. **The next milestone to add browser tests — M15b on current order — decides per 0020 §4's next rung before it is accepted**, and has no headroom left to defer with. **Rung 3 taken at M15b** (103 tests): quiet-machine line is now **22 s of 25 s** after large-world variants were born `@slow` — `join_at_max_zoom_out_never_drops` specifically, with a shrunk fast-tier variant (10 ticks) kept in the fast tier so ring backpressure stays covered there; the full 200-tick scenario moved to `@slow`. **M15c adds a readback test (`overlay_tile_reaches_screen`) and a zero-GC page (the panning window) and must measure the line and take the next rung before being accepted**; if no rung remains that does not demote a feature's only test, M15c's brief says to stop and report — that pulls M36b's suite audit forward, the orchestrator's call, not the implementer's | decided: 0026 at M09; wire reached again at M13b; rung 3 taken at M15b (22 s of 25 s, 103 tests); next rung is M15c's or M36b's |
| Is `attributedBytesPerFrame`-based attribution needed on `main` in software mode at all, once `software.frames: 600` (M10 step 4) removes 0016 caveat b's own stated precondition ("harness overhead no longer amortises" at a shrunk N)? Software mode could drop attribution entirely — for every isolate, `main` included — and read raw `bytesPerFrame` the same as hardware, which would also remove the residual V8-inlining exposure on `main`'s `drive` root that M10 accepted (with a nested control as a live detector) rather than closed. A simplification, not a defect: `main`'s current design is correct and tested (125/125, `--repeat-each 5`, both modes), just possibly no longer necessary. Deliberately left open by M10's own orchestrator rather than folded into that milestone's scope | Owned: question only, not a plan | M36b (suite audit) |
| **A non-zero `wasm` runner exit with a fully passing report, cause unknown; seen twice.** Second occurrence: M17b's `done` push, run [35904901201](https://github.com/tylerschloesser/engine-v2/actions/runs/35904901201), slow tier, `wasm FAIL 3 tests 33s` / "runner exited 1 after a parseable report showed 0 failures"; the re-push 20 minutes later was green. Both occurrences are slow-tier on CI. Third: M17c's `done` push, run 35939177480, same message (two of the last three pushes). **M17d step 0 landed a diagnostic, not a fix:** the `wasm` suite now logs Vitest's unhandled errors and exit reasons (shown by a forced occurrence), and it never reproduced locally. **Named at its next CI occurrence (run 35951386457) and fixed (`ba78b32`):** `watchCrate` (`src/vite.ts`) recursively watched the whole crate directory, `target/` included. On Linux, Node's JS recursive watcher hit `ENOENT` on a cargo temp file and emitted an unhandled `error`. It now watches `src/` recursively plus the manifests directly. **Closed.** Run [35619437805](https://github.com/tylerschloesser/engine-v2/actions/runs/35619437805): the slow-tier `wasm` step exited 1 while `wasm/report.json` itself showed `success: true`, `numFailedTests: 0`, all 3 `@slow` tests passed; `wasm/output.log` held exactly one line, Vitest's own built-in JSON-reporter confirmation, nothing else — no stack trace, no signal notice. **Not reproduced**: 4 consecutive local runs (`pnpm test:slow wasm`, this Mac) and the very next CI run ([35621295966](https://github.com/tylerschloesser/engine-v2/actions/runs/35621295966)), which was fully green on both tiers. A non-recurring failure with no identified cause is not fixed — recorded as open, not closed, specifically so a future session that hits it again starts from this evidence instead of rediscovering it. Leading explanation, **labelled a hypothesis, not a finding**: an unhandled rejection or exception outside the awaited test lifecycle (after a slow-tier `wasm` test file's own `afterAll` already resolved — `plugin-build.test.ts`, `worldgen-bench.test.ts` and `plugin-rebuild-error.test.ts` were all read for an unawaited promise or unclosed resource; nothing definitive found in any of the three), invisible because only `--reporter=json` is active (no `default` reporter alongside it to print whatever Vitest would otherwise say). What *is* fixed, permanently, regardless of cause: `scripts/lib/adapters.mjs`'s failure message now distinguishes "runner exited N after a parseable report showed 0 failures" from "without a parseable report" (M10, `pnpm test unit -t adapter`), so a recurrence will be reported accurately instead of pointing at a missing report that isn't missing | Owned: watch for recurrence; the corrected message (M10) is what lets it be recognised if it returns | M10 (found, message fixed); unowned beyond that — next session to see it recur |
| `Host::chunk_versions` (`BTreeMap<ChunkCoord, u32>`) grows one permanent entry per distinct chunk ever touched by a replicated write, ~26 B each, never pruned | Accepted as-is at M15's gate, recorded rather than fixed. Bounded by world state rather than by time, so not a leak — but it grows *faster* than world state does: in M15's measured window it gained 300 keys against 150 newly overlaid chunks, because a chunk earns a permanent version entry from an entity that merely passed through it and then left it pristine and empty. See `docs/plan/15-connection-and-subscriptions.md`'s Deviations, "Fix round 3b" item 2 | candidate owner M31b |
| Client-side cache-event queue (`Cache::events`) is never drained by `ClientCore`/`Replica` | Latent: not exercised by any of M15's own tests, none of which read a replica tile through the upload path. Reintroduces the same unbounded growth M15 fixed on the host, the moment a client-side store has cache events enabled without a drain alongside it. See `docs/plan/15-connection-and-subscriptions.md`'s Deviations, "Fix round 3" item 3 | owner 15b |
| `gc-sim-paced`'s `main` budget of 30 does not cover the interpreter worst case: `client.ts`'s `now()` costs ~11.96 B/frame when `main` runs interpreted, taking raw `main` to ~33.5 | Owned: the orchestrator measured this failing 1 run in 6 under `--load 10`, at the same rate on M15's base commit `ebea2a3` as on M15's head, so it is pre-existing and not M15's. M13b recorded the same `now()` cost as a candidate milestone of its own; this is the first measurement of what it actually costs the suite. **Do not widen the budget** — ADR 0029 makes a budget that stops a control tripping the exact failure mode it exists to avoid | open; see `docs/plan/questions-for-tyler.md` | **CLOSED by M15d** (`docs/plan/15d-client-clock-allocation.md`): the read is gone from the production frame path entirely — `Scheduler.requestFrame` already delivered a real rAF timestamp that the callback was discarding, so `tick(tMs)` now assigns it directly, no clock call and no box; `test/client.ts`'s `stepFrame` uses a resyncing clock (one real read per 30 calls). `sim-paced`'s `main` went 33.5 -> ~21.9 with both V8 tiers now reading the same, and the **software budget was re-derived downward 14 -> 9**, restoring 0016's ordinary +8 margin in place of M13b's ad hoc +2; hardware `main` held at 30. Controls still trip (60/60, 32/32, 75/75). Repeats: **15/15 plain** and **14/15 under `--load 10`**, with no `sim-paced` failure in either set, against 7/15 plain before.
| `parkWorkers: timed out after 10000 ms` under saturation | Seen once in 15 `browser` runs at `--load 10` at M15d's gate, on a run that took 28 s against a 25 s budget — a harness timeout, not an assertion, and consistent with 0020's own note that at high load every suite fails on timeouts only. **Not raised**: lengthening the timeout would mask the saturation rather than measure it. Watch item; if it appears without saturation, it is news. **At M16's gate it appeared without injected load, twice, on the same test** (`zero_gc_action neg object client`: 1 in 30 quiet runs, then 1 in 15), and that test went 20/20 run alone. So it is saturation inside the full suite, now at 27-32 s, not a defect in the page; the suite-time ladder being exhausted is the signal underneath | M16e instrumented it (`pollUntil` names each worker's state); leads owned by M16f. **At M17's gate: 1 in 60 (`zero_gc_action neg object sim`, quiet run, 0 in 30 under `--load 10`), and this time the stuck worker is `client`, not M16e's `gen0`:** `{"isolate":"client","W_YIELD":1,"W_PARKED":0,"W_WAKE":1776,"W_ACK":1500}` against `sim`/`gen0` both parked. The client saw the yield request and never parked while its wakes kept arriving, a different shape from M16e's caught-up `gen0` (wake == ack). **At M17b's gate it recurred with byte-identical counters** (`zero_gc_action neg object main`), so the stall sits at a fixed point in the page's workload | **Closed by M17c.** Two waits checked their park flag only *after* waiting: `worker/shell.ts` `runBlockingLoop` (a park landing inside `Shell.resume()` was absorbed into the first wait's baseline) and `test/harness-worker.ts` `armedLoop` (`parkOne`'s notify, which never changes `Req`, lost when no waiter was present). Both now check before every wait, and each has a deterministic test that fails on the old loop. 30/30 clean loops afterwards. These are the only two `Atomics.wait` sites in `src/` (`sab.atomics_wait_confined`). **M20c: does not explain this row.** A separate, *deterministic* hang was found and fixed on a `host: { kind: 'local', connect: true }` + manual-clock + no-real-frame-loop topology (docs/plan/20c-client-ack-freeze-under-untilquiescent.md): `engine/test.untilQuiescent` waited on `uploadRing`, a ring only a page's own renderer/test code ever drains, never a worker, so the wait never resolved -- the client worker itself was never stuck (`W_ACK` already equalled `CB_FRAME_REQ`; `worker/client-upload.ts`'s `pump()` never blocks on a full or undrained ring). Different shape from this row's own signature: there the error is `untilQuiescent: timed out` with `W_YIELD: 0` (`parkWorkers` was never even reached, since `untilQuiescent`'s own ring-quiescence check never returned true); this row's own captures are `parkWorkers: timed out` with `W_YIELD: 1` (a park request already sent, the worker not yet observing it while wakes keep arriving) under real suite saturation, intermittent, not reproduced on demand. Fixed at `packages/engine/src/test/client.ts`'s `ringSabs` (no longer includes `uploadRing`) |
| `ManualClock.frame()`/`.advance()` box a fresh `HeapNumber` on their own `now += ms` update, ~12 B/frame | Found at M15d's gate on every page using `ManualClock` (`gc-loop`, and the `gc-*` forms of `topology`/`gen`/`terrain`/`input`). Same defect class M13b and M15d each fixed elsewhere, currently absorbed by those pages' larger budgets and causing no red test. Reported, not chased. Also found: `gc-sim`'s own `main` at 28 B/frame under forced interpreter from `stepSimTickSync`'s Atomics-related box (not a clock read), currently covered by that page's budget | open; owner unassigned |
| Cache-invalidation gap in pre-existing M07/M08b code, found at M15b's gate: `Cache::evict_if_present` (`world/cache.rs`) frees a slot without calling `push_event`, unlike `materialize`'s own LRU-eviction path, and `GenQueue::set_view` (`gen_queue.rs`) returns early on unchanged `view.visible` alone, consulting no cache event at all — so a chunk `TerrainStore::replace_overlay`/`clear_overlay` correctly evicts never comes back resident while the camera holds still | Owned: fix scoped and scheduled. Undetectable before M15b because no code before M15b's `game_instance.rs` restructuring ever had a `Replica` and a `TerrainFeed` sharing one `TerrainStore` — see `docs/plan/15b-ring-connection-and-replica-rendering.md`'s Deviations, "Newly discovered bug, not fixed here (Decision needed)" | owned by M15c (`docs/plan/15c-terrain-visibility-and-cache-invalidation.md`) |
| `server.ts`'s `resync()` corrects the sim only when it is behind (`if (overshoot > 0)`); there is no branch for running ahead. `syncBaseMs += accountedTicks * tickMs` advances by the nominal amount regardless, so if anything ever makes `poll()` fire faster than the tick rate, the sim runs fast with nothing pulling it back — uncorrected, not bounded at one window the way ADR 0030's own text claims. Found at M15e's gate while repairing `poll_skips_a_spurious_tick_on_a_ring_wake`'s measurement; not reachable today (the measured error is in the other direction: a reverted `wokenBy` guard *adds* ticks, `resync()`'s blind spot is the ahead case) and not M15e's to fix | candidate owner M36b, or whichever milestone next touches pacing |
| `SimHost.runOneTick` allocates ~2.27 B/frame on a *connected* sim worker (1360 B over 600 frames) | Attributed by the orchestrator at M15c's gate with `windowByFn`, on the new `gc-connected-terrain` page, after the implementer's budget formula explained the isolate's 6.46-8.19 B/frame as "ordinary JIT/warm-up noise". It is not noise: `runOneTick` 1360 B, `warm` 1152 B, `resync` 900 B, three named frames summing to the measured rate. `warm` and `resync` are ADR 0030's two accepted `clock.now()` reads per resync window and are already understood; `runOneTick`'s share is new and appears only on a connected host tick (snapshot/downlink building over a live `RingConnection`), which no earlier zero-GC page exercised. Absorbed by this page's `sim: 17` budget, which is derived and documented, not widened to hide it. Same defect class M13b and M15d each fixed on other isolates | open; candidate owner M31 or M36b |
| A locally dropped action (malformed via `dispatchRaw`, or a full outbox) increments a ring drop counter but produces **no `onActionResult`**, so that `seq` never resolves on `main` | Accepted at M16's gate (`16-action-round-trip.md` Deviations, "a locally dropped action produced no `onActionResult`"). Real local rejection belongs to the pending queue | owned by M25 |
| With continuous real rendering plus `test.flags.pace`, `worker/sim.ts`'s `AtomicsTimer.poll()` can be starved for seconds under ADR 0030's wake guard. It was always seen to recover, never stuck for good | Observed on `slice.html` at M16 (Deviations, steps 6-7). **M16c measured it as a 0.4-2.9 s stall at tick 1, then a catch-up burst, on every run**: the fixed `timeoutMs()` restarts on every ring wake | **closed by M16d** (ADR 0032; `sim_ticks_steadily_under_external_wakes` reads 0 ticks in 2 s on the pre-fix timer, re-run by the orchestrator) |
| `engine/test.untilQuiescent` can resolve before a just-applied delta's upload has even been queued, so a GPU-residency probe that trusts it races | Found by M16d step 4 on `slice.html`: at the failing read the chunk was in the client store but the shader drew its not-resident colour (`32,32,32`). Fixed for `slice.ts` only (`__sliceSettle(tileX, tileY)` waits for residency and a quiet host). Any other page that probes pixels right after a delta has the same gap | open; candidate owner the next milestone adding a pixel probe after a delta (M17b or M18) |
| The client sends an uplink batch about every 50 ms right after a camera move, which looks at odds with 0010's "send only on change" | Observed by M16d while measuring wake rates, not investigated | open; candidate owner M19 (presence uplink) |
| `budgets.json`'s `sim-paced` `formula` string still quotes numbers from before M15b | Reported by M16d. Text only; the number itself is current | open; next milestone that edits `budgets.json` |
| `sim-paced`'s `AtomicsTimer` clock read happens about once per real tick while the page wakes the sim every frame: a per-time cost against a per-frame zero-GC ceiling (0.4-2.2 B/frame of a strict 8), so a slow enough machine could exceed it. Same shape as 0030's one resync read per window | Recorded in ADR 0032's Amendment at M16d's third CI round. JS has no integer clock that avoids the `HeapNumber` box. A slower CI runner is the likely first place to see it | open; watch; candidate owner M36b |
| Since M16d's third CI round, a resync window that just ran catch-up ticks skips chunk warming (0030 §3 warmer). Under sustained catch-up, warming could be deferred indefinitely | ADR 0032 Amendment. Not observed; `chunksWarmed` is the counter to read | open; watch, owner unassigned |
| `connected-terrain neg burst {client,sim,gen0} @slow` (hardware) failed 3/3 with `main` at 111.07-111.74 against 111, at a 1-minute load of 10.7 | Did not reproduce at load 6.6 (orchestrator) or in eight attempts including 40-way oversubscription (M16f): `main` 103.3-103.8. The budget's 8 B margin is used up only when a sibling burst coincides with high ambient load. CI's software slow tier is green | open; watch. If it recurs, force the budget to 1 at that moment to dump `windowByFn` (M16f Deviations, step 3) |
| `pnpm test`'s build step reads `WARN 32-34s/30s` (35-42 s by M17b's gate) (`buildBudgetMs`) at M17's gate on every run, including warm ones; M17 added a fixture crate (`fx-drawables`) to `build-fixtures.mjs`. A warning, not a failure; per-step timings are not logged, so which step grew is unmeasured | Recorded at M17's gate, not attributed | **Closed by M17d** (ADR 0033): two causes fixed, warm build 7.3 s |
| `gc-test` skill: a production-topology page that drives `stepFrame` outside `window.__gc.run()` must `resume()` parked workers first (a parked worker answers only a `resume` message, not a bare wake). Found by M17's implementer as a hang in the `drawables` page's population loop | M17 Deviations, steps 4-6 | open; fold into the skill at the next zero-GC page |
| The fast `browser` suite reads 24 s of its 25 s budget at M17b's gate (144 tests; 21 s at M17's start). ADR 0020 §4's demotion ladder was already exhausted at M15c, and M16c was the last suite-time milestone. The next milestone that adds fast browser tests (M18, M19) will cross it | Recorded at M17b's gate | **Closed by M17d:** ADR 0033 sets `browser` at 35 s inside the one-minute total (`pnpm test` 30 s wall) |
| `terrain: chunks generate, upload and evict inside the window` failed once on CI's fast tier (SwiftShader, run 35951386457 attempt 1: `CHUNK records must upload inside the window`; generation did happen inside the window) and passed on the rerun | Recorded at M17d's CI round; never seen locally | open; watch. If it recurs, read what `uploadedChunks` counts against the 600-frame window before touching the test |
| A pointer slot reused by a second press right after a completed multi-frame longpress can inherit stale `wasActive`/`heldMs` state (`input/semantic.ts`, M11 code) | Found by M18's review agent; recorded, not fixed (M18 Deviations, steps 7-8) | open; owner unassigned. Candidate: the next milestone that touches `semantic.ts` (M20b's collect UI or M33's construction mode) |
| `fixtures/terrain`'s hand-written `frame()` no longer clears `InputQueue` (M18 moved the input drain before `frame()`, so clearing there erased same-wake input). A long-running `fx-terrain` page (`device.html`, `gc-input`) fills the fixed 64-slot queue and then drops the incoming event silently. Fixture-only: `GameInstance<G>` clears at the end of every `frame()` | M18 steps 4-6 gate round 1 | accepted; revisit if a fixture page ever reads the queue past 64 events |
| `input.game_record_survives_overflow` never reaches "64 `kind::GAME` events already queued + 1 more" | M18 Deviations (acknowledged) | open; M33 (first real `emit` consumer) |
| `stepping: 1,000 stepTick()` returned a wrong sim hash once in 30 quiet full-suite runs at M18's gate (never before). Did not recur in 195 more runs (M18c) | M18c committed a diagnostic: a mismatch now prints `expected`/`actual`/`ticksRun`/`req`/`ack`/`state`/`yield` | open; watch. When it recurs, read `ticksRun` against `req` first: a lost tick versus a doubled tick versus a hash taken mid-work are now distinguishable |
| `host_admit_path_allocates_zero_bytes_per_action` read 900 B net over its 100-action window once on CI (run 36087861610, a doc-only commit); 0 in 500 local runs (300 quiet, 200 under load). No `HashMap`/`RandomState` on the host path | M19c committed a failure-only diagnostic: `high_water_bytes()` before/after and every non-zero per-tick delta in the panic message | open; watch. When it recurs, read which tick and which phase the bytes landed in before guessing a mechanism |
| `park('sim'): timed out` while `State` is `Armed` and `Req` = `Ack`, on `gc: flat transport parity`: 1 in 15 quiet full-suite runs at both M18's and M19's gates, 0 in 58 under `--load 10` | Recorded at M18's and M19's gates | **Closed by M19b**: `parkOne` notified `Req` without changing it; now a shared `Wake` word (`signalPark`/`signalWake`) |
| `RemotePresenceEntry::arrived_ms` is tick-derived, not wall-clock arrival (`ClientCore::on_frame` takes no time) | M19 Deviations | M30 threads a real arrival time |
| A harness worker that crashes after setup never rejects a pending `parkOne`/`send` promise (`worker.onerror` still closes over the long-settled setup promise), so it surfaces as a `park`/`send` timeout rather than as the crash | M19b Deviations, step 1 candidate 2 | open; owner unassigned. Read this first if a future `park` timeout comes with evidence that the worker died |
| `bench.frame_worstcase @slow` (CI software smoke mode) read `record_count` 65,408 against 65,536 after warm-up (one `BATCH_COLS` batch short) on M19b's `done` push, attempt 1; the re-run passed. The same symptom M17b's CI round 1 "fixed" with a second trailing `stepFrame`, so setup's final downlink drain is still timing-dependent | Recorded at M19b's CI | open; the next session's first item (`PROMPT.md` Exact next step) |
