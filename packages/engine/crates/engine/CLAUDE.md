# engine crate (Rust side)

The crate game authors path-depend on; it and the game crate compile into one WASM module. It ships inside the npm package (`files` lists `crates`).

## Workspace

A member of the root cargo workspace. These live at the repo root, not here: profiles and `[workspace.lints]` (`Cargo.toml`), the clippy ban lists (`clippy.toml`, owned by `docs/decisions/0002-determinism-same-wasm-everywhere.md` §3), the toolchain pin (`rust-toolchain.toml`), nextest profiles (`.config/nextest.toml`). This crate's `Cargo.toml` inherits `version`, `edition`, `publish` and `lints` from the workspace.

`[profile.dev.package."*"]` covers only non-member dependencies, so this crate builds at the dev `opt-level` (0024 §13).

## Commands

- `pnpm test rust [-t <name>]`: nextest over the workspace; `-t` is a substring of the test name. Raw `cargo nextest run` and `cargo test` also work.
- `pnpm lint` runs `cargo fmt --check` and `cargo clippy --workspace --all-targets -- -D warnings`. `pnpm format` fixes formatting.
- On macOS, native linking needs `DEVELOPER_DIR` when the Xcode licence is not accepted; the scripts set it (`scripts/lib/env.mjs`). For a bare `cargo` call that links: `DEVELOPER_DIR=/Library/Developer/CommandLineTools cargo …`.

## Layout

- `src/abi/`: the JS↔WASM boundary (0014). `registry.rs` is the single owner of the ABI and states the rule for adding to it; `mod.rs` holds what the exports do; `boot.rs`, `regions.rs`, `arena.rs`, `panic.rs`, `config.rs` are the pieces. `panic::fatal` exists because std formats a panic message into a `String` before the hook runs: use it, not `panic!`, anywhere the allocator may be the failure. Sim exports (docs/plan/13-sim-host-tick-loop.md, docs/plan/15b-ring-connection-and-replica-rendering.md): `sim_genesis() -> status` (builds the world from the init config; a loaded world instead calls `sim_restore_*`/`sim_replay_*` below), `sim_seal_frame() -> len` (write-ahead log bytes into `RegionId::Persist`; real since docs/plan/22-persistence-log-and-snapshots.md steps 4-6), `sim_segment_header(segment, base_tick) -> len`, `sim_snapshot_begin(segment, offset) -> status`/`sim_snapshot_next() -> len` (the streaming snapshot pair), `sim_dirty() -> u32` (all four also steps 4-6), `sim_warm_one() -> u32` (1/0, the warmer's between-tick chunk generator), `tick_hz() -> u32` (the sim role's real tick rate, `20` default, `Host<G>`/`GameInstance<G>` overriding it as `G::TICK_RATE.hz_value()` -- `SimHost`, `packages/engine/src/server.ts`, reads it once at construction instead of assuming 20); `sim_connect(conn) -> status`/`sim_disconnect(conn) -> status` (M15b: `host::Host::connect`/`disconnect`); `sim_tick`/`sim_hash`/`sim_admit(conn, len)`/`sim_build_frame(conn) -> len` are all real once a role-`Sim` `Instance` exists (M15b routes the last two, plus `sim_tick`, through `Host`'s connection-aware `tick`/`build_frame`/`seal`, not the bare `Sim::step` M13 left them at). docs/plan/22b-persistence-load-and-fs.md (`ABI_VERSION` 16 -> 17): `sim_restore_begin(total_len) -> status`/`sim_restore_push(len) -> status`/`sim_restore_end() -> status` (decodes a snapshot fed through `RegionId::Persist` reused as a receive region, writes `logSegment`/`logOffset` into `Result` on success), `sim_replay_begin(segment, offset) -> status`/`sim_replay_push(len) -> status`/`sim_replay_end() -> status`/`sim_replay_valid_end() -> u32` (replays a log tail the same way, `Status::TornTail` on a bad block, not fatal for the currently open segment), `sim_tick_now() -> u32`. New statuses: `IdentityMismatch`, `Corrupt`, `ContainerVersion`, `TornTail`. `ABI_VERSION` was 10 as of M15b steps 4-6 (client-role `on_frame(len) -> status`/`client_poll_uplink(t_ms) -> len`, `RegionId::Downlink`, test-only `sim_region_hash`/`client_region_hash`/`sim_conn_counters`); later milestones through docs/plan/17-drawlist-and-sprites.md raised it to 15, docs/plan/22-persistence-log-and-snapshots.md steps 4-6 to 16, docs/plan/22b-persistence-load-and-fs.md to 17, and docs/plan/24-recovery-and-migration.md to 18 then 19 (`sim_replay_scan_begin(segment)`/`sim_replay_scan_push(len)`/`sim_replay_scan_end()` -- the `Skip`-target scan pass, over the same bytes `sim_replay_begin`/`push`/`end` then apply -- `sim_log_skip(segment, offset) -> len`, `sim_test_trap() -> status` and `sim_reattach(conn) -> status` (`host::Host::reattach`: re-attaches a connection with the same deterministic `PlayerId` a live `sim_connect` would assign, but queues no `Record::Player` event and touches no game state -- recovery's own call after re-instantiation, Traps "Connections stay open across recovery"; forwarded through `GameInstance<G>` like every other sim export, `game_instance.rs`); `RegionId::Progress` (11), `persist::{Phase, ProgressCursor}`, written by `Host<G>` before every risky call and readable from a *dead* instance with no export call) (`registry.rs` is the ABI's own source of truth if this line drifts again).
- `src/host/` (M13, connections M15, ABI-real M15b): `Host<G>` -- the sim-role `Instance` (`Sim<G>` plus `sim_genesis`/`sim_seal_frame`/`sim_warm_one`'s own state) plus a fixed connection table (`connect`/`disconnect`/`on_uplink`/`tick`/`seal`/`build_frame`/`region_hash`), and `host::warm::Warm` (the between-tick warmer: a fixed-capacity per-connection view list, `set_view`/`warm_one` over `view::nearest_first` -- `build_frame` feeds it a real subscribed rect every call). `host::subs::SubscriptionSet` (M15, 0010): ring-1-plus-look-ahead subscription, hysteresis, the 128-chunk cap. `src/client/` (M15, docs/plan/15-connection-and-subscriptions.md): `client::Replica<G>` (the replicated state a client holds: `Store<G>` + held-chunk versions, `WorldRead<G>` directly, `Unknown` outside the held set) and `client::ClientCore<G>` (`on_frame` decodes a frame twice -- once to validate, once to apply, so a malformed tail never mutates the replica -- `poll_uplink` paces the uplink per 0010 "Rates"). **`host/` and `client/` are outside the deterministic core: they may read subscriptions and cameras; `sim/` (`sim.rs`, `authority.rs`, `store.rs`, `world/`, `worldgen/`, ...) may not import them** -- enforced by `tests/module_layering.rs`. `src/game_instance.rs` (M13): `GameInstance<G>`, the `Instance` `export_game!` now points at -- dispatches `Role::Sim` to `Host<G>`, `Role::Gen` to `worldgen::GenCore<G::Worldgen>`, `Role::Client` to `ClientInstance<G>` (a generic `TerrainStore`/`TerrainFeed`/`Uploader`/`InputQueue` assembly; existing fixtures with their own hand-written `Instance` impls, `fixtures/terrain` and `fixtures/worldgen`, are untouched). `Host::init` declares the sim role's own `Rx`/`Tx` regions now (M15b: `SIM_RX_BYTES` 4,096, `SIM_TX_BYTES` 65,536, both provisional -- 0010's own bandwidth/backpressure budget for them is Non-scope, M31). `ClientInstance<G>` (M15b steps 4-6) now holds a boxed `client::ClientCore<G>` instead of its own standalone `TerrainStore`: `Replica<G>`'s own store is the one `TerrainFeed`/`Uploader` share too (`Replica::terrain`/`terrain_mut`, `pub(crate)`), so `on_frame`'s dispatch can drain `Replica`'s dirty queue (`client::DirtyEvent::{Whole, Tile}`, one `Vec`, not two -- a first attempt with two separate queues reintroduced the exact unbounded-growth bug M15 fixed for cache events, since only one of `drain_dirty`/`drain_dirty_for_upload` is ever called per instance) straight into `Uploader::enqueue_chunk`/`patch_tile`.
- `src/hash.rs`: `Fnv64`. `src/testing/` (feature `testing`, dev-dependencies only): `golden_bytes` (`assert_golden`, `assert_golden_bytes!`/`assert_golden_hash!`), `cache_matrix` (below), `testkit` (`run_script`: drives a `Sim<G>` through a `(Tick, Record)` script, idling any gap, returns the final `state_hash`; `Loopback<G>`, M15: one `Host`, `K` `ClientCore`s, real wire bytes end to end, each client behind its own constant tick delay -- freely imports both `host` and `client`, unlike the deterministic core), `budgets` (M15: `budget`/`expect_within_budget`, the Rust-side reader for `packages/engine/budgets.json`, mirroring `tests/support/budgets.ts`).
- `src/world/` (0007): `tile.rs`/`coords.rs`/`traits.rs` are plain data; `overlay.rs` is the sparse per-chunk state (`ChunkOverlay`, `Overlays`); `cache.rs` is the dense LRU slab pool (`CacheCapacity`/`CacheEvent` are its only public seam); `terrain.rs`'s `TerrainStore` composes them. **The cache is not state**: never serialized, hashed, or part of `modified_tiles`; `TerrainStore::tile`/`materialize` take `&self` (a `RefCell` inside) because a read may generate and evict. No method here returns a reference into a slab -- reads return `Tile` by value, `copy_chunk` copies out.
- Adding or changing anything that touches the cache: prove invisibility with `engine::testing::assert_cache_invisible(|cfg| { .. build a store at cfg.capacity, prewarm with cfg.prewarm, run a script, return checkpoints .. })` (feature `testing`) -- it replays your closure at capacity 1, the default and `Unlimited`, crossed with three prewarm orders, and fails if any leg's checkpoints differ. `world_cache_invisible.rs`'s `cache_invisible_matrix` is the reference caller.
- `src/game.rs` (0003, M12): the `Game` trait, ids (`PlayerId`, `EntityId`), `Unknown`, `PlayerEvent`, `OldStore`/`SaveIncompatible` shells (still M24b's to fill), and `pub use` re-exports of `WorldRead`/`WorldWrite` (`src/world_access.rs`), `TickCx` (`src/authority.rs`), `FrameCx`/`FrameView`/`DrawList` (`src/client/`) and `Presence`/`PresenceTable` (`src/presence.rs`, M19) so every caller's `crate::game::{WorldRead, WorldWrite, TickCx, Presence, PresenceTable, ...}` import path stays the same regardless of which module actually defines them. `src/presence.rs` (M19, docs/plan/19-presence-channel.md steps 1-3): `Presence`/`PresenceEntry`/`PresenceTable<G>` (`get`/`iter` public, `on_sample`/`remove`/`restore` engine-internal by convention -- a compile-fail doc test on `PresenceTable` proves `Game::apply`'s own signature cannot name it). **Presence types never enter `Store`, the log or a hash**: `apply`/`tick` have no `PresenceTable` parameter at all, so the deterministic core has no way to read one even if it wanted to. `src/time.rs`: `Tick`, `Ticks`, `TickRate` (the 0006 conversions -- `hz`/`millis`/`secs`/`dt` -- are M12b's). `src/rng.rs`: `SimRng` (hand-rolled PCG32, 0002 "Randomness"). `src/delta.rs`: the engine-defined `Delta<G>` (0011, plus the engine-only `Roster` and `Ack` variants of 0024 §8 and M12b respectively -- `Ack` is never rebroadcast to a client, see `authority.rs`). `src/store/mod.rs` (M12, M21, M21b): `Store<G>` -- tile overlays (`TerrainStore`), entities, players, global, the `Registry` `Game::register` fills once at construction, every chunk's `ChunkIndex`, and (M21b) the timer wheel/wake queue/active lists (`crate::sim::{timers, wake, active}`) -- whose only mutator is `apply(&Delta<G>)`; canonical `encode`/`decode` and `impl StateHash`. **`store/index.rs`'s `ChunkIndex` (occupancy bitset + sorted `(index, EntityId)` + overlapping ids) is derived, never encoded or hashed** (0007 §5): maintained incrementally by `Store::apply`'s `EntityPut`/`EntityGone` arms (skipped entirely when a put's anchor and footprint are unchanged from before, M21b: property-only updates -- every timer/wake/active-list put among them -- used to destroy and rebuild a lone occupant's whole `ChunkIndex` on every call), rebuilt from the entity table by `Store::rebuild_indexes` (called automatically at the end of `decode`) -- the same derived/state split `world/cache.rs`'s own dense slabs already draw. **The timer wheel/wake queue/active lists are sim state, not derived** (0007 §7): encoded/hashed (active lists, then timers, then the wake queue's own `next` list -- Store::encode gains them; the wake queue's placement, not named in 0005, is this milestone's own choice), but a client replica's own `Store` never receives any of the three over the wire, so it always encodes as empty there. `src/budget.rs` (M21): the state-budget check + growth-honesty audit (0004, 0023), called from `Sim::step` -- deliberately *not* under `host/`, since `sim/` may not import it (`tests/module_layering.rs`).
- `src/world_access.rs` (0003, M12b): `WorldRead`/`WorldWrite`, filled in exactly as 0003 (object-safe, trait-object upcasting); `View`, the read-only host/replica context (a "held chunk" predicate, always true on the host). `src/authority.rs` (M12b, M21b): `Authority<G>` (`Store` + `SimRng` + `Tick`, since M12's `Store` deliberately holds neither), `Scope`/`Scopes`/`ChangeLog` (scope derivation at write time), and `TickCx` (`as_write`/`player_count`/`player_id_at` from M12b; M21b's own `next_woken`/`next_due`/`wake`/`wake_at`/`cancel_wake`/`activate`/`deactivate`/`active_len`/`active_at`, its final shape). `src/sim/mod.rs` (M12b; a directory since M21b, siblings `sim/timers.rs`/`sim/wake.rs`/`sim/active.rs`, `pub(crate)`-only): `Sim<G>`, the host driver -- `genesis` builds a fresh world and runs `Game::genesis` once; `step` runs one frame's records in 0004 order (each successful `Authority`-side entity put outside `G::tick` auto-wakes, 0007 §7), then the fixed point (`Authority::begin_tick`: swaps the wake queue, compacts every active list's tombstones from the *previous* tick), then `Game::tick`, then `Authority::end_tick` (drops whatever the wake queue's `now` list is left holding), then advances the tick. **Puts made through `TickCx` never auto-wake** (only `Authority`'s own `WorldWrite` impl does, reached by `apply`/`on_player`/`genesis`) -- it is why `TickCx` and `Authority` are distinct types (0003, M12b), and it is the whole reason `Machines::tick`'s own smelt cycle (`fixtures/machines`) has to call `TickCx::wake_at` explicitly rather than relying on its own `put_entity` calls. **The two author rules from 0003** ("validate first, write after" and "copy-modify-put; add a `?` to each read and `impl From<Unknown> for Reject`") aren't just prose here: the first is enforced by the undo journal (`authority.rs`'s `UndoJournal`, adopted, [0037](../../../../docs/decisions/0037-undo-journal-adopted.md)) in release builds (rolls back a rejecting `apply` that wrote, counts it in `Authority::apply_rollbacks`) and by an unconditional panic in debug/test builds regardless (`a rejecting apply recorded a write`), proven both ways by `fx-puts`'s `tests/puts_apply_contract.rs` (a clean reject, and a deliberately bad test-only game that writes then rejects) and by `crates/engine/tests/undo_journal.rs` (the rollback itself, via a test-only bypass).
- `src/client/drawlist.rs` (M17, 0018 §2; docs/plan/18-picking-and-overlay.md steps 4-6 add `anchor`/`ANCHOR_SLOTS` and `sort_into`'s own `follow` parameter): `Draw`/`DrawList` -- the per-frame draw list `ClientSide::extract` fills through builder methods (`sprite`/`circle`/`ring`/`rect`/`bar`/`radial`/`ghost`/`anchor`), counting-sorted by `layer` into a `RegionId::DrawList`-shaped buffer by `sort_into` (`game_instance.rs`'s own follow-up call inside `frame()`, not part of the game-facing surface), which also writes the header's `follow_valid`/`follow` (absolute world tiles) and `anchor_mask`/`anchors` (tiles relative to `window_origin`, like `Draw::pos`) fields. **`extract` is pure over `FrameView`: iterate `entities()`, never allocate; positions are `WorldPos`, the engine subtracts the window origin.** `src/client/frame_view.rs`: `FrameView`/`Clocks`/`EntityIter` (M16b's minimal shape, grown by M17 with `entities()`, `visible()`, `zoom()`, `px_per_tile()`, `cursor_tile()`, `window_origin()`, `time_ms()`). `src/client/frame_cx.rs` (docs/plan/18-picking-and-overlay.md steps 4-6): `FrameCx<'a, G>`, `ClientSide::frame`'s own per-frame context -- **`frame` mutates (`cx.follow(..)`, `cx.ui_dirty()`), `extract` reads (`&FrameView`)**; `cx.input()` is the slice of `client::input::InputEvent`s (`InputQueue::events()`) drained from `inputRing` since the previous `frame` call, oldest first, valid for that call only -- `game_instance.rs`'s `GameInstance::frame` builds one `FrameCx`, calls `G::Client::frame`, then reads back `cx`'s own `follow`/`ui_dirty` state before `extract`/`sort_into` run, and clears `InputQueue` only after all of that. `InputEvent::kind == client::input::kind::GAME` (7) is `client.input.emit(code, a, b)`'s own record (`code()`/`a()`/`b()` accessors over `pick_id`/`tile`); `InputQueue`'s overflow policy never drops an already-queued `GAME` event.
- Determinism rules for everything here: `.claude/rules/determinism.md`. `world/cache.rs` also carries `.claude/rules/hot-paths.md` (reads are per-tick).

## Tests

- Unit tests inline (`#[cfg(test)] mod tests`); scenario and replay tests in `tests/*.rs`.
- Slow tier: name the test function `slow_*`. `pnpm test` filters it out; `pnpm test:slow` runs only those.
- `tests/runner_control.rs` is the runner's permanent negative control (fails only under `pnpm test --self-check-fail`). Keep it.
- `cargo fmt --check` only sees files in a target's module tree: a stray `.rs` outside `src/` or `tests/` is never checked.

## Dependencies

Policy and the exact allowed list: `docs/decisions/0017-packaging-and-build.md` §7. Anything else needs an ADR (`write-adr` skill). Dev-dependencies are unrestricted.

## One crate, for now

`crates/` holds one crate named `engine`. The triggers for splitting it (compile budget, lint scope, a proc-macro) and the constraints on any split are in `docs/plan/01-scaffolding.md`, Planning decisions (b), until an ADR replaces them.
