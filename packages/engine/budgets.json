{
  "version": 1,
  "gc": {
    "pages": {
      "gc-loop": {
        "isolates": {
          "main": {
            "class": "strict",
            "bytesPerFrame": 54,
            "formula": "no WebGPU on this page (0016 §1's 110 B floor does not apply): measured harness overhead, ceil(max of 50 clean runs = 45.43-45.55 B/frame) = 46, + 8 B margin (0016 §1's own margin convention) = 54",
            "attributionRoots": ["run"]
          },
          "sim": {
            "class": "strict",
            "bytesPerFrame": 8,
            "formula": "0016 §1: the strict worker figure. Measured clean here: constant 2.97 B/frame (1784 B / 600) over 50 runs, comfortably under the 8 B figure the spike's minimal worker measured at 0.99 B/frame",
            "attributionRoots": ["coreTick"]
          }
        },
        "software": {
          "frames": 600,
          "isolates": {
            "main": {
              "attributedBytesPerFrame": 28
            },
            "sim": {
              "attributedBytesPerFrame": 8
            }
          }
        }
      },
      "topology": {
        "isolates": {
          "main": {
            "class": "strict",
            "bytesPerFrame": 52,
            "formula": "0016 §1 margin convention (docs/plan/06b-workers-and-spawn.md): measured max of 20 clean runs at `warmupFrames: 8000` = 43.64 B/frame (higher than gc-loop's own main since this page's drive also locksteps sim+gen0 through asHarness.stepTick), ceil = 44, + 8 B margin = 52. `warmupFrames: 8000` (Deviations): at gc-loop's own 120, and even at 3000, this page's deeper call chain had not fully reached steady optimized code -- main measured as high as 51.8 B/frame and occasionally spiked (once to 53.86 on `echo`), which also masked the `object` negative control; 8000 warmup frames (~50 ms extra, never counted) removed both the spikes and the extra baseline.",
            "attributionRoots": ["stepFrame", "stepTick"]
          },
          "client": {
            "class": "strict",
            "bytesPerFrame": 33,
            "formula": "0016 §1 margin convention: measured max of 43 clean runs = 24.97 B/frame (bimodal ~17-25 B/frame across runs; real work, not JIT noise -- `frame()`/camera-block read each stepped frame), ceil = 25, + 8 B margin = 33.",
            "attributionRoots": ["body"]
          },
          "sim": {
            "class": "strict",
            "bytesPerFrame": 8,
            "formula": "0016 §1: the strict worker figure (same as gc-loop's own sim entry). Measured max of 20 clean runs at `warmupFrames: 8000` = 7.11 B/frame, comfortably under 8.",
            "attributionRoots": ["body"]
          },
          "gen0": {
            "class": "strict",
            "bytesPerFrame": 8,
            "formula": "0016 §1: the strict worker figure. Identical measured shape to `sim` (both run the same `asHarness.stepTick` lockstep body): max of 20 clean runs = 7.11 B/frame.",
            "attributionRoots": ["body"]
          }
        },
        "software": null
      },
      "echo": {
        "isolates": {
          "main": {
            "class": "strict",
            "bytesPerFrame": 40,
            "formula": "0016 §1 margin convention: measured max of 20 clean runs at `warmupFrames: 8000` = 31.78 B/frame (the earlier 120/3000-warmup runs saw a 53.86 outlier and no separation from the `object` control; see `topology`'s own `main` formula), ceil = 32, + 8 B margin = 40.",
            "attributionRoots": ["drive"]
          },
          "client": {
            "class": "strict",
            "bytesPerFrame": 35,
            "formula": "0016 §1 margin convention: measured max of 35 clean runs = 26.81 B/frame (stable across runs: the client worker's echo body, `worker/client.ts`), ceil = 27, + 8 B margin = 35.",
            "attributionRoots": ["body"]
          },
          "sim": {
            "class": "strict",
            "bytesPerFrame": 8,
            "formula": "0016 §1: the strict worker figure. Measured max of 20 clean runs at `warmupFrames: 8000` = 7.59 B/frame, comfortably under 8.",
            "attributionRoots": ["body"]
          },
          "gen0": {
            "class": "strict",
            "bytesPerFrame": 8,
            "formula": "0016 §1: the strict worker figure. Identical measured shape to `sim`: max of 20 clean runs = 7.59 B/frame.",
            "attributionRoots": ["body"]
          }
        },
        "software": null
      }
    }
  },
  "counters": {
    "sab": {
      "totalBytes": 12582912,
      "formula": "docs/decisions/0015-threads-memory-and-topology.md §5's ~12 MiB SAB budget line, in bytes (12 * 1024 * 1024). Measured sab/layout.ts sabBytesTotal() (two gen workers, the worst case): 8,277,684 B (~7.9 MiB) -- control 256 B + camera 80 B + clock seqlock 36 B + drawList triple 3*(1,024 + 2,097,152) B + six single rings + two gen-worker ring pairs (docs/plan/06-sab-primitives-and-workers.md, Ring capacities)."
    }
  }
}
