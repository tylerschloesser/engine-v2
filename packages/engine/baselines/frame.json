{
  "recordCount": 65536,
  "frames": 304,
  "warmupFrames": 120,
  "measuredAt": "2026-09-23T18:20:00Z",
  "conditions": "Tyler's Mac (Mac15,10, 14 logical CPUs), channel: 'chromium' (real Metal adapter headless), --enable-unsafe-webgpu --disable-frame-rate-limit --disable-gpu-vsync, playwright.config.ts's 'frame-bench' project, load average ~3-6 (mixed; five consecutive runs averaged as medians below). frame-bench.html: fx-drawables, host: { kind: 'local', connect: true }, genWorkers: 1, camera tilesAcross=256 (0018 §6 maximum zoom-out), halfExtentTilesX/Y=130 (independent of tilesAcross/viewport aspect -- written verbatim into CameraBlock.half_extent_tiles and read directly by FrameView::visible(), not derived from viewport pixels) over a 256x256 grid of 65,536 sprite-drawing entities. Fix round 1 (coordinator review): recordCount/dropped are now read from DrawablesRenderer's own live published-slot header (window.__frameBench.recordCount()/dropped()), asserted === 65536 / === 0 after warm-up and again at the end of the timed window in bench.frame_worstcase -- verified by injection (a temporary halfExtentTilesX/Y=70 produced a real, caught shortfall of 21,025 records) that the check is not vacuous. The real worst case was already being measured (the record count was never actually short); this update replaces a page-side constant comparison with a real one and re-measures under it. main/worker p50/p95 are the median of five consecutive clean runs' own medians (main p50 spread 0.619-0.646ms, worker p50 spread 2.121-2.237ms) over 304 mf-s/mf-e mark pairs and ~22-24 wf-s/wf-e mark pairs respectively per run (fewer on the worker side because, under uncapped rAF, main vastly outpaces the worker's own ~2.1-2.3ms/call cost at this record count, coalescing several main frames' worth of CB_FRAME_REQ advances into each worker wake, same as production tolerates per 0018 §1).",
  "mainMs": { "p50": 0.637, "p95": 0.691 },
  "workerMs": { "p50": 2.152, "p95": 2.344 }
}
